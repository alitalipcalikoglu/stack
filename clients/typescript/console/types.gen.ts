/**
 * GENERATED FILE -- do not edit by hand.
 * Produced from <repo>/openapi.yaml by `node clients/typescript/generate.mjs`.
 * Re-run generation instead of patching this file; see clients/typescript/README.md.
 */
/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/health": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Liveness probe.
         * @description Always `200` once the process is up. No dependency check and no authentication. Hand-rolled here rather than using `service-core`'s `registerProbes`.
         */
        get: operations["console.health.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/ready": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Readiness probe.
         * @description Runs `db.ping()` on every call — uncached, unlike the backend services' shared probe helper, because the check is a single cheap SQLite read.
         */
        get: operations["console.ready.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/info": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Service identity and capabilities.
         * @description The one operational route console takes from the shared `registerInfo` helper, so its shape is identical across all 13 services.
         */
        get: operations["console.info.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/session/login": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * First step of sign-in: email and password.
         * @description Pre-session: no cookie and no CSRF header are required. Always issues a session cookie on success. When the account has TOTP enabled the response is `{ totpRequired: true, admin: null }` and the new session is *pending* — it resolves on later requests but `requireSession` rejects it with `401 TOTP_REQUIRED` until `POST /api/session/totp` completes it. Rate-limited per client IP before any password work happens. Failures are deliberately indistinguishable between "no such email" and "wrong password" (both `401 INVALID_CREDENTIALS`, both pay the same hashing cost).
         */
        post: operations["console.session.login"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/session/totp": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Second step of sign-in: TOTP code.
         * @description Pre-session in the same sense as login: no CSRF header is required and `requireSession` is never called — but the pending session cookie from `POST /api/session/login` must already be present, otherwise the answer is `401 UNAUTHENTICATED`. Rate-limited per client IP. A wrong code counts towards the same lockout counter as a wrong password, so enough bad codes lock the account (`423`) and destroy the pending session.
         */
        post: operations["console.session.totp"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/session": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Current session state.
         * @description Never rejects: with no cookie, an expired one, or an unknown token it answers `200 { admin: null, totpPending: false }`; with a pending session `{ admin: null, totpPending: true }`. The app uses it to decide between the login form, the TOTP form and the console itself.
         */
        get: operations["console.session.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/session/logout": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Sign out.
         * @description The one mutating route with no CSRF header requirement: it does not call `requireSession`, it checks `request.admin`/`request.consoleSession` by hand, so the `x-console-request` gate never runs. Documented here as it really behaves — Phase 0 flagged the exemption as a design question for the team, and this document does not paper over it. It also never rejects: with no session it just clears the cookie and answers `204`. The blast radius is limited to a forced sign-out (the `SameSite=Strict` cookie still means a cross-site form post carries no session, so a third-party page cannot even reach an authenticated version of this route).
         */
        post: operations["console.session.logout"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/me/sessions": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List my active sessions.
         * @description Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.me.sessions.list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/me/sessions/logout-others": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Revoke my other sessions.
         * @description Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        post: operations["console.me.sessions.logoutOthers"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/me/password": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Change my password.
         * @description Verifies the current password first, enforces the strength rules on the new one, then revokes every other session for this administrator.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        post: operations["console.me.password.change"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/me/totp/start": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Begin TOTP enrolment.
         * @description Generates and stores a new secret that is not enforced until `POST /api/me/totp/confirm` succeeds. The secret is returned exactly once, in this response.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        post: operations["console.me.totp.start"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/me/totp/confirm": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Confirm TOTP enrolment.
         * @description Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        post: operations["console.me.totp.confirm"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/me/totp/disable": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Disable TOTP.
         * @description Requires both the current password and a valid code.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        post: operations["console.me.totp.disable"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/admins": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List console administrators.
         * @description Readable by any signed-in session, including a `viewer`.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.admins.list"];
        put?: never;
        /**
         * Create a console administrator.
         * @description **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         */
        post: operations["console.admins.create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/admins/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Delete an administrator.
         * @description Irreversible. Refused for your own account, and for the last active administrator.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         */
        delete: operations["console.admins.delete"];
        options?: never;
        head?: never;
        /**
         * Update an administrator's name, role or status.
         * @description Two guards beyond validation: the last active administrator can be neither demoted nor disabled, and nobody can demote or disable their own account.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         */
        patch: operations["console.admins.update"];
        trace?: never;
    };
    "/api/admins/{id}/password": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Set another administrator's password.
         * @description **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         */
        post: operations["console.admins.resetPassword"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/admins/{id}/unlock": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Clear a lockout.
         * @description Resets the failed-login counter and the lockout deadline.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         */
        post: operations["console.admins.unlock"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/audit": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read console's own admin-activity log.
         * @description console's local log of what administrators did in this console — distinct from the audit service reached through `/api/services/{sid}/audit/*`. Entries are also forwarded to the first configured audit service when one has a write-capable key. Keyset pagination: pass the previous page's `nextBefore` as `before`. Default page size 50.
         *
         *     Query validation uses console's shared `paging` schema — the one query schema in the service with `additionalProperties: true`, so unrecognized query keys are accepted by validation and then silently ignored; only documented parameters are ever forwarded downstream.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.activity.list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List configured service instances.
         * @description The authoritative list of valid `sid` values for this deployment, with each instance's type — which is what decides the set of proxy routes it answers.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.services.list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/overview": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Probe and summarize every service.
         * @description Fans out to every configured service in parallel: `/health`, `/ready` and its metrics-derived summary. One failing service degrades its own entry only and never fails the request.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.services.overview"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/about": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Collect every service's `/v1/info`.
         * @description Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.services.about"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/settings": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Change a service's console-side polling settings.
         * @description Purely console-side state, persisted to `services.json`; the service itself is never contacted. Note the `404` here is console's own `NOT_FOUND`, not the proxy layer's `UNKNOWN_SERVICE`.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         */
        patch: operations["console.services.updateSettings"];
        trace?: never;
    };
    "/api/services/{sid}/status": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Probe one service.
         * @description Works for any configured service whatever its type. The `summary` is the typed client's metrics digest; if collecting it throws, `summary` becomes `{ "error": "..." }` and the request still succeeds.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.services.status"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/notify/messages": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List notification messages.
         * @description Typed pass-through to the **notify** service instance named by `sid` — console relays this to its `GET /v1/messages`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Of the accepted query parameters only `status`, `limit`, `cursor` are forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Query validation uses console's shared `paging` schema — the one query schema in the service with `additionalProperties: true`, so unrecognized query keys are accepted by validation and then silently ignored; only documented parameters are ever forwarded downstream.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.notify.messages.list"];
        put?: never;
        /**
         * Send a message.
         * @description Typed pass-through to the **notify** service instance named by `sid` — console relays this to its `POST /v1/messages`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.notify.messages.send"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/notify/messages/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get one message.
         * @description Typed pass-through to the **notify** service instance named by `sid` — console relays this to its `GET /v1/messages/{id}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.notify.messages.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/notify/messages/{id}/retry": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Retry a failed message.
         * @description Typed pass-through to the **notify** service instance named by `sid` — console relays this to its `POST /v1/messages/{id}/retry`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.notify.messages.retry"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/notify/templates": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List message templates.
         * @description Typed pass-through to the **notify** service instance named by `sid` — console relays this to its `GET /v1/templates`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.notify.templates.list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/auth/users": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List users.
         * @description Typed pass-through to the **auth** service instance named by `sid` — console relays this to its `GET /v1/users`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Of the accepted query parameters only `email`, `limit`, `cursor` are forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Query validation uses console's shared `paging` schema — the one query schema in the service with `additionalProperties: true`, so unrecognized query keys are accepted by validation and then silently ignored; only documented parameters are ever forwarded downstream.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.auth.users.list"];
        put?: never;
        /**
         * Create a user.
         * @description Typed pass-through to the **auth** service instance named by `sid` — console relays this to its `POST /v1/users`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.auth.users.create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/auth/users/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get a user with sessions and recent events.
         * @description Typed pass-through to the **auth** service instance named by `sid` — console relays this to its `GET /v1/users/{id} + /sessions + /events`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.auth.users.get"];
        put?: never;
        post?: never;
        /**
         * Delete a user.
         * @description Typed pass-through to the **auth** service instance named by `sid` — console relays this to its `DELETE /v1/users/{id}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        delete: operations["console.auth.users.delete"];
        options?: never;
        head?: never;
        /**
         * Update a user's name or status.
         * @description Typed pass-through to the **auth** service instance named by `sid` — console relays this to its `PATCH /v1/users/{id}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        patch: operations["console.auth.users.update"];
        trace?: never;
    };
    "/api/services/{sid}/auth/users/{id}/events": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Page a user's events.
         * @description Typed pass-through to the **auth** service instance named by `sid` — console relays this to its `GET /v1/users/{id}/events`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Of the accepted query parameters only `limit`, `before` are forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Query validation uses console's shared `paging` schema — the one query schema in the service with `additionalProperties: true`, so unrecognized query keys are accepted by validation and then silently ignored; only documented parameters are ever forwarded downstream.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.auth.users.events"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/auth/users/{id}/sessions": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Revoke all of a user's sessions.
         * @description Typed pass-through to the **auth** service instance named by `sid` — console relays this to its `DELETE /v1/users/{id}/sessions`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        delete: operations["console.auth.users.revokeAllSessions"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/auth/users/{id}/sessions/{sub}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Revoke one session of a user.
         * @description Typed pass-through to the **auth** service instance named by `sid` — console relays this to its `DELETE /v1/users/{id}/sessions/{sessionId}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        delete: operations["console.auth.users.revokeSession"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/auth/users/{id}/resend-verification": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Resend a verification email.
         * @description Typed pass-through to the **auth** service instance named by `sid` — console relays this to its `POST /v1/auth/verify-email/resend`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.auth.users.resendVerification"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/auth/users/{id}/password-reset-email": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Send a password-reset email.
         * @description Typed pass-through to the **auth** service instance named by `sid` — console relays this to its `POST /v1/auth/password/forgot`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.auth.users.passwordResetEmail"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/auth/jwks": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Fetch the auth service's JWKS.
         * @description Typed pass-through to the **auth** service instance named by `sid` — console relays this to its `GET /.well-known/jwks.json`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     The one proxy call console makes with no API key at all (`auth: none`) — the JWKS is public by design.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.auth.jwks.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/media/files": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List stored files.
         * @description Typed pass-through to the **media** service instance named by `sid` — console relays this to its `GET /v1/files`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Of the accepted query parameters only `limit`, `cursor` are forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Query validation uses console's shared `paging` schema — the one query schema in the service with `additionalProperties: true`, so unrecognized query keys are accepted by validation and then silently ignored; only documented parameters are ever forwarded downstream.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.media.files.list"];
        /**
         * Upload file bytes through the console.
         * @description Typed pass-through to the **media** service instance named by `sid` — console relays this to its `PUT /v1/files`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     The one route registered directly on the root Fastify instance rather than inside the `/api` plugin, so it escapes the 64 KiB JSON body limit that applies everywhere else: it declares its own 512 MiB limit and a wildcard content-type parser that hands the raw stream straight to the media client without buffering. The request's `content-type` and `content-length` are relayed as sent; the downstream call gets a 300 s timeout instead of the usual 10 s. Same caveat Phase 0 recorded for media itself: the Fastify `bodyLimit` is not what actually enforces the cap for a raw passthrough parser — the real per-byte limit lives in media's storage layer.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        put: operations["console.media.files.uploadTicketed"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/media/files/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get one file's metadata.
         * @description Typed pass-through to the **media** service instance named by `sid` — console relays this to its `GET /v1/files/{id}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.media.files.get"];
        put?: never;
        post?: never;
        /**
         * Delete a file.
         * @description Typed pass-through to the **media** service instance named by `sid` — console relays this to its `DELETE /v1/files/{id}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        delete: operations["console.media.files.delete"];
        options?: never;
        head?: never;
        /**
         * Rename a file or change its visibility.
         * @description Typed pass-through to the **media** service instance named by `sid` — console relays this to its `PATCH /v1/files/{id}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        patch: operations["console.media.files.update"];
        trace?: never;
    };
    "/api/services/{sid}/media/files/{id}/bytes/{sub}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Stream a file's bytes through the console.
         * @description Typed pass-through to the **media** service instance named by `sid` — console relays this to its `GET the signed variant URL`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Same-origin byte relay for the console UI. Hardened deliberately: only `image/jpeg`, `image/png`, `image/webp`, `image/gif` and `image/avif` keep their real content type and any `content-disposition` media sent; everything else (SVG, HTML, PDF, video, ...) is rewritten to `application/octet-stream` with `attachment; filename="{id}.bin"` so nothing can execute or render in console's origin. Every response also carries `content-security-policy: default-src 'none'; sandbox` and `cache-control: private, max-age=300`. `{sub}` is the variant name.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.media.files.bytes"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/media/files/{id}/restore": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Restore a soft-deleted file.
         * @description Typed pass-through to the **media** service instance named by `sid` — console relays this to its `POST /v1/files/{id}/restore`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.media.files.restore"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/media/files/{id}/urls": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Mint signed URLs for a file.
         * @description Typed pass-through to the **media** service instance named by `sid` — console relays this to its `POST /v1/files/{id}/urls`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     A `POST` that only reads — it mints short-lived URLs and changes nothing — which is why it needs a session rather than the `admin` role. It is still a `POST`, so the CSRF header is required.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.media.files.urls"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/media/tickets": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Create a direct-upload ticket.
         * @description Typed pass-through to the **media** service instance named by `sid` — console relays this to its `POST /v1/uploads`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.media.tickets.create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/audit/events": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Query audit events.
         * @description Typed pass-through to the **audit** service instance named by `sid` — console relays this to its `GET /v1/events`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.audit.events.list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/audit/events/export": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Export audit events as NDJSON or CSV.
         * @description Typed pass-through to the **audit** service instance named by `sid` — console relays this to its `GET /v1/events/export`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Streams the downstream export straight through, relaying its `content-type` and `content-disposition` and adding `content-security-policy: default-src 'none'; sandbox`. A non-2xx downstream response becomes a `ServiceError` rather than a partial file. The export itself is recorded in console's activity log, with the filter that produced it.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.audit.events.export"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/audit/events/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get one audit event.
         * @description Typed pass-through to the **audit** service instance named by `sid` — console relays this to its `GET /v1/events/{id}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.audit.events.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/audit/stats": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Audit counters.
         * @description Typed pass-through to the **audit** service instance named by `sid` — console relays this to its `GET /v1/stats`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Of the accepted query parameters only `hours` is forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.audit.stats.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/audit/chain/head": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Current hash-chain head.
         * @description Typed pass-through to the **audit** service instance named by `sid` — console relays this to its `GET /v1/chain/head`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.audit.chain.head"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/audit/chain/verify": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Verify the hash chain.
         * @description Typed pass-through to the **audit** service instance named by `sid` — console relays this to its `GET /v1/chain/verify`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     A `GET` with a side effect on console's side: the verification outcome (`ok`, `checked`, `firstBroken`) is written to console's own activity log. Being a `GET` it needs no CSRF header.
         *
         *     Of the accepted query parameters only `fromSeq`, `toSeq` are forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.audit.chain.verify"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/shortlink/links": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List short links.
         * @description Typed pass-through to the **shortlink** service instance named by `sid` — console relays this to its `GET /v1/links`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Of the accepted query parameters only `q`, `tag`, `status`, `createdBy`, `limit`, `cursor` are forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.shortlink.links.list"];
        put?: never;
        /**
         * Create a short link.
         * @description Typed pass-through to the **shortlink** service instance named by `sid` — console relays this to its `POST /v1/links`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.shortlink.links.create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/shortlink/links/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get a short link with its stats.
         * @description Typed pass-through to the **shortlink** service instance named by `sid` — console relays this to its `GET /v1/links/{code} + /stats`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Of the accepted query parameters only `days` (passed to the stats call) is forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.shortlink.links.get"];
        put?: never;
        post?: never;
        /**
         * Delete a short link.
         * @description Typed pass-through to the **shortlink** service instance named by `sid` — console relays this to its `DELETE /v1/links/{code}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        delete: operations["console.shortlink.links.delete"];
        options?: never;
        head?: never;
        /**
         * Update a short link.
         * @description Typed pass-through to the **shortlink** service instance named by `sid` — console relays this to its `PATCH /v1/links/{code}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Note `slug` is creation-only: it is accepted by `POST` and rejected here (`additionalProperties: false`).
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        patch: operations["console.shortlink.links.update"];
        trace?: never;
    };
    "/api/services/{sid}/shortlink/links/{id}/stats": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Stats for one short link.
         * @description Typed pass-through to the **shortlink** service instance named by `sid` — console relays this to its `GET /v1/links/{code}/stats`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Of the accepted query parameters only `days` is forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.shortlink.links.stats"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/shortlink/links/{id}/qr.png": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * QR code for a short link, as PNG.
         * @description Typed pass-through to the **shortlink** service instance named by `sid` — console relays this to its `GET /v1/links/{code}/qr?format=png`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     PNG only — console pins `format=png` and never relays the SVG variant, so nothing scriptable can be served from its own origin. Buffered whole (not streamed) and served with `cache-control: private, max-age=3600` and `content-security-policy: default-src 'none'; sandbox`.
         *
         *     Of the accepted query parameters only `scale`, `margin` are forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.shortlink.links.qr"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/shortlink/stats": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Service-wide short-link stats.
         * @description Typed pass-through to the **shortlink** service instance named by `sid` — console relays this to its `GET /v1/stats`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Of the accepted query parameters only `days` is forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.shortlink.stats.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/flags/environments": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List flag environments.
         * @description Typed pass-through to the **flags** service instance named by `sid` — console relays this to its `GET /v1/environments`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.flags.environments.list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/flags/stats": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Flag counters.
         * @description Typed pass-through to the **flags** service instance named by `sid` — console relays this to its `GET /v1/stats`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.flags.stats.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/flags/flags": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List feature flags.
         * @description Typed pass-through to the **flags** service instance named by `sid` — console relays this to its `GET /v1/flags`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.flags.flags.list"];
        put?: never;
        /**
         * Create a feature flag.
         * @description Typed pass-through to the **flags** service instance named by `sid` — console relays this to its `POST /v1/flags`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.flags.flags.create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/flags/flags/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get a flag with its recent history.
         * @description Typed pass-through to the **flags** service instance named by `sid` — console relays this to its `GET /v1/flags/{key} + /history`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.flags.flags.get"];
        put?: never;
        post?: never;
        /**
         * Delete a flag.
         * @description Typed pass-through to the **flags** service instance named by `sid` — console relays this to its `DELETE /v1/flags/{key}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        delete: operations["console.flags.flags.delete"];
        options?: never;
        head?: never;
        /**
         * Update a flag's metadata.
         * @description Typed pass-through to the **flags** service instance named by `sid` — console relays this to its `PATCH /v1/flags/{key}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Metadata only. Values, rules and percentages are per environment — use the environment patch route.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        patch: operations["console.flags.flags.update"];
        trace?: never;
    };
    "/api/services/{sid}/flags/flags/{id}/history": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Page one flag's change history.
         * @description Typed pass-through to the **flags** service instance named by `sid` — console relays this to its `GET /v1/flags/{key}/history`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Of the accepted query parameters only `limit`, `before` are forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.flags.flags.history"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/flags/history": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Page the service-wide change history.
         * @description Typed pass-through to the **flags** service instance named by `sid` — console relays this to its `GET /v1/history`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Of the accepted query parameters only `limit`, `before` are forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.flags.history.list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/flags/flags/{id}/envs/{sub}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Update a flag in one environment.
         * @description Typed pass-through to the **flags** service instance named by `sid` — console relays this to its `PATCH /v1/flags/{key}/envs/{env}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        patch: operations["console.flags.envs.update"];
        trace?: never;
    };
    "/api/services/{sid}/flags/flags/{id}/envs/{sub}/copy": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Copy a flag's configuration between environments.
         * @description Typed pass-through to the **flags** service instance named by `sid` — console relays this to its `POST /v1/flags/{key}/envs/{env}/copy`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     `{sub}` is the source environment, the body's `to` is the destination.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.flags.envs.copy"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/flags/evaluate": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Evaluate flags for a context.
         * @description Typed pass-through to the **flags** service instance named by `sid` — console relays this to its `POST /v1/evaluate`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Read-only evaluation, so a `viewer` may call it — but it is a `POST`, so the CSRF header is still required.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.flags.evaluate.post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/scheduler/stats": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Scheduler counters.
         * @description Typed pass-through to the **scheduler** service instance named by `sid` — console relays this to its `GET /v1/stats`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.scheduler.stats.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/scheduler/target-keys": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List configured target keys.
         * @description Typed pass-through to the **scheduler** service instance named by `sid` — console relays this to its `GET /v1/target-keys`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.scheduler.targetKeys.list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/scheduler/timezones": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List supported timezones.
         * @description Typed pass-through to the **scheduler** service instance named by `sid` — console relays this to its `GET /v1/timezones`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.scheduler.timezones.list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/scheduler/preview": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Preview upcoming fire times for a cron expression.
         * @description Typed pass-through to the **scheduler** service instance named by `sid` — console relays this to its `GET /v1/preview`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     A missing `cron` is forwarded as the empty string — scheduler, not console, produces the error for it.
         *
         *     Of the accepted query parameters only `cron`, `timezone`, `count` are forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.scheduler.preview.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/scheduler/jobs": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List scheduled jobs.
         * @description Typed pass-through to the **scheduler** service instance named by `sid` — console relays this to its `GET /v1/jobs`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.scheduler.jobs.list"];
        put?: never;
        /**
         * Create a scheduled job.
         * @description Typed pass-through to the **scheduler** service instance named by `sid` — console relays this to its `POST /v1/jobs`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.scheduler.jobs.create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/scheduler/jobs/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get a job with its recent runs.
         * @description Typed pass-through to the **scheduler** service instance named by `sid` — console relays this to its `GET /v1/jobs/{name} + /v1/runs`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.scheduler.jobs.get"];
        put?: never;
        post?: never;
        /**
         * Delete a scheduled job.
         * @description Typed pass-through to the **scheduler** service instance named by `sid` — console relays this to its `DELETE /v1/jobs/{name}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        delete: operations["console.scheduler.jobs.delete"];
        options?: never;
        head?: never;
        /**
         * Update a scheduled job.
         * @description Typed pass-through to the **scheduler** service instance named by `sid` — console relays this to its `PATCH /v1/jobs/{name}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     `name` is creation-only and cannot be patched.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        patch: operations["console.scheduler.jobs.update"];
        trace?: never;
    };
    "/api/services/{sid}/scheduler/jobs/{id}/run": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Trigger a job now.
         * @description Typed pass-through to the **scheduler** service instance named by `sid` — console relays this to its `POST /v1/jobs/{name}/run`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.scheduler.jobs.run"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/scheduler/jobs/{id}/runs": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Page one job's runs.
         * @description Typed pass-through to the **scheduler** service instance named by `sid` — console relays this to its `GET /v1/runs?job={name}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Of the accepted query parameters only `status`, `limit`, `before` are forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.scheduler.jobs.runs"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/scheduler/runs": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Page runs across jobs.
         * @description Typed pass-through to the **scheduler** service instance named by `sid` — console relays this to its `GET /v1/runs`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Of the accepted query parameters only `job`, `status`, `limit`, `before` are forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.scheduler.runs.list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/scheduler/runs/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get one run.
         * @description Typed pass-through to the **scheduler** service instance named by `sid` — console relays this to its `GET /v1/runs/{id}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.scheduler.runs.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/scheduler/runs/{id}/cancel": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Cancel a run.
         * @description Typed pass-through to the **scheduler** service instance named by `sid` — console relays this to its `POST /v1/runs/{id}/cancel`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.scheduler.runs.cancel"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/webhook-out/stats": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Webhook counters.
         * @description Typed pass-through to the **webhook-out** service instance named by `sid` — console relays this to its `GET /v1/stats`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.webhookOut.stats.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/webhook-out/event-types": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List known event types.
         * @description Typed pass-through to the **webhook-out** service instance named by `sid` — console relays this to its `GET /v1/event-types`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.webhookOut.eventTypes.list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/webhook-out/subscriptions": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List subscriptions.
         * @description Typed pass-through to the **webhook-out** service instance named by `sid` — console relays this to its `GET /v1/subscriptions`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.webhookOut.subscriptions.list"];
        put?: never;
        /**
         * Create a subscription.
         * @description Typed pass-through to the **webhook-out** service instance named by `sid` — console relays this to its `POST /v1/subscriptions`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.webhookOut.subscriptions.create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/webhook-out/subscriptions/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get a subscription with its recent deliveries.
         * @description Typed pass-through to the **webhook-out** service instance named by `sid` — console relays this to its `GET /v1/subscriptions/{id} + /v1/deliveries`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.webhookOut.subscriptions.get"];
        put?: never;
        post?: never;
        /**
         * Delete a subscription.
         * @description Typed pass-through to the **webhook-out** service instance named by `sid` — console relays this to its `DELETE /v1/subscriptions/{id}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        delete: operations["console.webhookOut.subscriptions.delete"];
        options?: never;
        head?: never;
        /**
         * Update a subscription.
         * @description Typed pass-through to the **webhook-out** service instance named by `sid` — console relays this to its `PATCH /v1/subscriptions/{id}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Unlike scheduler's job patch, `name` IS patchable here — console accepts the same field set for create and patch.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        patch: operations["console.webhookOut.subscriptions.update"];
        trace?: never;
    };
    "/api/services/{sid}/webhook-out/subscriptions/{id}/rotate": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Rotate a subscription's signing secret.
         * @description Typed pass-through to the **webhook-out** service instance named by `sid` — console relays this to its `POST /v1/subscriptions/{id}/rotate`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.webhookOut.subscriptions.rotate"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/webhook-out/subscriptions/{id}/test": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Send a test delivery.
         * @description Typed pass-through to the **webhook-out** service instance named by `sid` — console relays this to its `POST /v1/subscriptions/{id}/test`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.webhookOut.subscriptions.test"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/webhook-out/subscriptions/{id}/replay": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Replay a time range of events to a subscription.
         * @description Typed pass-through to the **webhook-out** service instance named by `sid` — console relays this to its `POST /v1/subscriptions/{id}/replay`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.webhookOut.subscriptions.replay"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/webhook-out/deliveries": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Page deliveries.
         * @description Typed pass-through to the **webhook-out** service instance named by `sid` — console relays this to its `GET /v1/deliveries`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Of the accepted query parameters only `status`, `subscription`, `event`, `limit`, `before` are forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.webhookOut.deliveries.list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/webhook-out/deliveries/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get one delivery.
         * @description Typed pass-through to the **webhook-out** service instance named by `sid` — console relays this to its `GET /v1/deliveries/{id}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.webhookOut.deliveries.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/webhook-out/deliveries/{id}/redeliver": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Redeliver a delivery.
         * @description Typed pass-through to the **webhook-out** service instance named by `sid` — console relays this to its `POST /v1/deliveries/{id}/redeliver`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.webhookOut.deliveries.redeliver"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/webhook-out/deliveries/{id}/cancel": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Cancel a pending delivery.
         * @description Typed pass-through to the **webhook-out** service instance named by `sid` — console relays this to its `POST /v1/deliveries/{id}/cancel`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.webhookOut.deliveries.cancel"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/webhook-out/events": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Page emitted events.
         * @description Typed pass-through to the **webhook-out** service instance named by `sid` — console relays this to its `GET /v1/events`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Of the accepted query parameters only `type`, `limit`, `before` are forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.webhookOut.events.list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/webhook-out/events/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get one emitted event.
         * @description Typed pass-through to the **webhook-out** service instance named by `sid` — console relays this to its `GET /v1/events/{id}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.webhookOut.events.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/search/stats": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Search counters.
         * @description Typed pass-through to the **search** service instance named by `sid` — console relays this to its `GET /v1/stats`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.search.stats.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/search/indexes": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List indexes.
         * @description Typed pass-through to the **search** service instance named by `sid` — console relays this to its `GET /v1/indexes`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.search.indexes.list"];
        put?: never;
        /**
         * Create an index.
         * @description Typed pass-through to the **search** service instance named by `sid` — console relays this to its `POST /v1/indexes`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.search.indexes.create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/search/indexes/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get one index.
         * @description Typed pass-through to the **search** service instance named by `sid` — console relays this to its `GET /v1/indexes/{name}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.search.indexes.get"];
        put?: never;
        post?: never;
        /**
         * Delete an index.
         * @description Typed pass-through to the **search** service instance named by `sid` — console relays this to its `DELETE /v1/indexes/{name}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        delete: operations["console.search.indexes.delete"];
        options?: never;
        head?: never;
        /**
         * Update an index.
         * @description Typed pass-through to the **search** service instance named by `sid` — console relays this to its `PATCH /v1/indexes/{name}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        patch: operations["console.search.indexes.update"];
        trace?: never;
    };
    "/api/services/{sid}/search/indexes/{id}/clear": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Remove every document from an index.
         * @description Typed pass-through to the **search** service instance named by `sid` — console relays this to its `POST /v1/indexes/{name}/clear`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.search.indexes.clear"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/search/indexes/{id}/search": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Run a search.
         * @description Typed pass-through to the **search** service instance named by `sid` — console relays this to its `POST /v1/indexes/{name}/search`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Read-only, so a `viewer` may search — but it is a `POST`, so the CSRF header is required.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.search.indexes.search"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/search/indexes/{id}/documents": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Browse documents in an index.
         * @description Typed pass-through to the **search** service instance named by `sid` — console relays this to its `GET /v1/indexes/{name}/documents`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Of the accepted query parameters only `limit`, `offset` are forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.search.documents.browse"];
        /**
         * Upsert documents.
         * @description Typed pass-through to the **search** service instance named by `sid` — console relays this to its `PUT /v1/indexes/{name}/documents`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        put: operations["console.search.documents.upsert"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/search/indexes/{id}/documents/{sub}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get one document.
         * @description Typed pass-through to the **search** service instance named by `sid` — console relays this to its `GET /v1/indexes/{name}/documents/{docId}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.search.documents.get"];
        put?: never;
        post?: never;
        /**
         * Delete one document.
         * @description Typed pass-through to the **search** service instance named by `sid` — console relays this to its `DELETE /v1/indexes/{name}/documents/{docId}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        delete: operations["console.search.documents.delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/ratelimit/stats": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Rate-limit counters.
         * @description Typed pass-through to the **ratelimit** service instance named by `sid` — console relays this to its `GET /v1/stats`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.ratelimit.stats.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/ratelimit/policies": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List policies.
         * @description Typed pass-through to the **ratelimit** service instance named by `sid` — console relays this to its `GET /v1/policies`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.ratelimit.policies.list"];
        put?: never;
        /**
         * Create a policy.
         * @description Typed pass-through to the **ratelimit** service instance named by `sid` — console relays this to its `POST /v1/policies`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.ratelimit.policies.create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/ratelimit/policies/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get one policy.
         * @description Typed pass-through to the **ratelimit** service instance named by `sid` — console relays this to its `GET /v1/policies/{name}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.ratelimit.policies.get"];
        put?: never;
        post?: never;
        /**
         * Delete a policy.
         * @description Typed pass-through to the **ratelimit** service instance named by `sid` — console relays this to its `DELETE /v1/policies/{name}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        delete: operations["console.ratelimit.policies.delete"];
        options?: never;
        head?: never;
        /**
         * Update a policy.
         * @description Typed pass-through to the **ratelimit** service instance named by `sid` — console relays this to its `PATCH /v1/policies/{name}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        patch: operations["console.ratelimit.policies.update"];
        trace?: never;
    };
    "/api/services/{sid}/ratelimit/policies/{id}/stats": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Counters for one policy.
         * @description Typed pass-through to the **ratelimit** service instance named by `sid` — console relays this to its `GET /v1/policies/{name}/stats`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.ratelimit.policies.stats"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/ratelimit/policies/{id}/top": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Top subjects for a policy.
         * @description Typed pass-through to the **ratelimit** service instance named by `sid` — console relays this to its `GET /v1/policies/{name}/top`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.ratelimit.policies.top"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/ratelimit/policies/{id}/overrides": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List per-subject overrides.
         * @description Typed pass-through to the **ratelimit** service instance named by `sid` — console relays this to its `GET /v1/policies/{name}/overrides`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.ratelimit.overrides.list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/ratelimit/policies/{id}/overrides/{sub}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * Set an override for one subject.
         * @description Typed pass-through to the **ratelimit** service instance named by `sid` — console relays this to its `PUT /v1/policies/{name}/overrides/{subject}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        put: operations["console.ratelimit.overrides.set"];
        post?: never;
        /**
         * Remove an override.
         * @description Typed pass-through to the **ratelimit** service instance named by `sid` — console relays this to its `DELETE /v1/policies/{name}/overrides/{subject}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        delete: operations["console.ratelimit.overrides.delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/ratelimit/policies/{id}/subjects/{sub}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Current usage for one subject.
         * @description Typed pass-through to the **ratelimit** service instance named by `sid` — console relays this to its `GET /v1/policies/{name}/subjects/{subject}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.ratelimit.subjects.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/ratelimit/policies/{id}/subjects/{sub}/usage": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Reset one subject's usage.
         * @description Typed pass-through to the **ratelimit** service instance named by `sid` — console relays this to its `DELETE /v1/policies/{name}/subjects/{subject}/usage`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        delete: operations["console.ratelimit.subjects.resetUsage"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/ratelimit/check": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Check (and by default consume) a rate-limit budget.
         * @description Typed pass-through to the **ratelimit** service instance named by `sid` — console relays this to its `POST /v1/check`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Admin-gated because the default (`peek` absent or false) really does consume budget. Mind the semantics ratelimit itself defines: a `200` can carry `allowed: false` — a denial is a successful HTTP response, not an error status.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.ratelimit.check.post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/geo/stats": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Geo counters.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `GET /v1/stats`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.geo.stats.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/geo/database": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Loaded geo database details.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `GET /v1/database`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.geo.database.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/geo/database/reload": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Reload the geo databases from disk.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `POST /v1/database/reload`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.geo.database.reload"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/geo/ip": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Look up one IP address.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `GET /v1/ip`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.geo.ip.lookup"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/geo/ip/batch": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Look up up to 100 IP addresses.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `POST /v1/ip/batch`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Read-only, so a `viewer` may call it; being a `POST` it still needs the CSRF header.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.geo.ip.batch"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/geo/countries": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List or search countries.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `GET /v1/countries`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.geo.countries.list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/geo/countries/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get one country.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `GET /v1/countries/{code}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Of the accepted query parameters only `lang` is forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.geo.countries.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/geo/currencies": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List currencies.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `GET /v1/currencies`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Of the accepted query parameters only `lang` is forwarded downstream; the rest belong to the shared query schema this route reuses and are ignored here.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.geo.currencies.list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/geo/timezones": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List or search timezones.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `GET /v1/timezones`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.geo.timezones.list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/geo/phone": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Parse a phone number.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `GET /v1/phone`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.geo.phone.lookup"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/geo/distance": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Distance between two coordinate pairs.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `GET /v1/distance`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.geo.distance.get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/geo/collections": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List place collections.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `GET /v1/collections`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.geo.collections.list"];
        put?: never;
        /**
         * Create a place collection.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `POST /v1/collections`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.geo.collections.create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/geo/collections/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get one collection.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `GET /v1/collections/{name}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.geo.collections.get"];
        put?: never;
        post?: never;
        /**
         * Delete a collection.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `DELETE /v1/collections/{name}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        delete: operations["console.geo.collections.delete"];
        options?: never;
        head?: never;
        /**
         * Update a collection's description.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `PATCH /v1/collections/{name}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        patch: operations["console.geo.collections.update"];
        trace?: never;
    };
    "/api/services/{sid}/geo/collections/{id}/clear": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Remove every place from a collection.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `POST /v1/collections/{name}/clear`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        post: operations["console.geo.collections.clear"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/geo/collections/{id}/places": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Page a collection's places.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `GET /v1/collections/{name}/places`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.geo.places.list"];
        /**
         * Upsert places into a collection.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `PUT /v1/collections/{name}/places`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        put: operations["console.geo.places.upsert"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/geo/collections/{id}/places/{sub}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Delete one place.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `DELETE /v1/collections/{name}/places/{placeId}`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     **Requires the `admin` role** (`requireAdmin`): a signed-in `viewer` gets `403 FORBIDDEN`.
         *
         *     Recorded in console's own activity log (`GET /api/audit`).
         */
        delete: operations["console.geo.places.delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/services/{sid}/geo/collections/{id}/nearby": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Find places near a point.
         * @description Typed pass-through to the **geo** service instance named by `sid` — console relays this to its `GET /v1/collections/{name}/nearby`. There is no generic proxy anywhere in console: this route is hand-written, individually schema-validated and individually role-gated.
         *
         *     Requires any signed-in session (`requireSession`); both `admin` and `viewer` may call it.
         */
        get: operations["console.geo.collections.nearby"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        ErrorEnvelope: {
            error: {
                /** @description Stable machine-readable code. console domain codes (`ConsoleError.STATUS`): INVALID_CREDENTIALS (401), UNAUTHENTICATED (401), TOTP_REQUIRED (401), INVALID_TOTP (401), TOTP_UNAVAILABLE (503), TOTP_SECRET_CORRUPT (500), ACCOUNT_LOCKED (423), ACCOUNT_DISABLED (403), FORBIDDEN (403), NOT_FOUND (404), EMAIL_TAKEN (409), CONFLICT (409), WEAK_PASSWORD (400), INVALID_ARGUMENT (400), LAST_ADMIN (409), RATE_LIMITED (429). Cross-cutting codes from console's own error handler: VALIDATION_FAILED (400, Ajv schema failure), NOT_FOUND (404, unmatched `/api/*` route), REQUEST_ERROR (other 4xx), INTERNAL_ERROR (500, message never leaked). */
                code: string;
                message: string;
                /** @description Present on Ajv validation failures as `[{ path, message }]` — note console hand-rolls its error handler and, unlike the shared `service-core` helper every backend service uses, omits `params` from those entries. Also present on domain errors that carry structured context: `retryAfterSec` (RATE_LIMITED, ACCOUNT_LOCKED) and `problems` (WEAK_PASSWORD). */
                details?: unknown;
            };
        };
        /** @description Error envelope produced by console's `ServiceError` branch, i.e. every failure that originated in — or while reaching — a downstream service. It is the plain `ErrorEnvelope` plus one extra field, `service`, naming the service instance that failed. `service` is absent when the failure is console-side routing rather than a downstream response (`UNKNOWN_SERVICE`, thrown by `ServiceClients#get` before any service is picked). */
        DownstreamErrorEnvelope: {
            error: {
                /** @description The downstream service's own `error.code` when it sent one; otherwise `UNKNOWN_SERVICE` (404, unknown `sid` or an instance of the wrong type), `UPSTREAM_UNREACHABLE` (502, transport failure or timeout), `UPSTREAM_ERROR` (downstream 5xx with no parseable code) or `UPSTREAM_REJECTED` (downstream 4xx with no parseable code). */
                code: string;
                message: string;
                /** @description The failing service instance id (`sid`). Absent for `UNKNOWN_SERVICE`. */
                service?: string;
                /** @description The downstream service's own `error.details`, forwarded verbatim when present. */
                details?: unknown;
            };
        };
        /** @description Raw JSON body of the downstream service's own response, relayed unchanged. console declares no Fastify `response:` schema anywhere, so it neither validates nor reshapes these bodies — the authoritative schema is the target service's own OpenAPI document for the operation named in the response description. Deliberately left open here rather than hand-copied, since nothing in console enforces a copy and it would drift silently. */
        PassThrough: {
            [key: string]: unknown;
        };
        Health: {
            /** @constant */
            status: "ok";
        };
        ReadyUnavailable: {
            /** @constant */
            status: "unavailable";
            /** @description The `db.ping()` failure message. */
            error?: string;
        };
        Info: {
            /** @constant */
            service: "console";
            /** @description This package's version. */
            version: string;
            /** @constant */
            apiVersion: "v1";
            /** @description Static, hardcoded for console: `["totp", "admin-roles", "audit-trail", "service-proxy"]`. Not instance-accurate — `totp` is listed even when `SECRETS_KEY` is unset (in which case enrolment fails with `503 TOTP_UNAVAILABLE`), and `service-proxy` says nothing about which services this instance actually has configured. Use `GET /api/services` for the real per-instance picture. */
            capabilities: string[];
            /** @description The open SQLite schema version (`PRAGMA user_version`). */
            schemaVersion: number;
            /** @description Version of the `@atc-web/service-core` copy actually running. Console imports `registerInfo` from `@atc-web/service-core/fastify`, so unlike `gateway` (the one service in this platform with no `service-core` dependency, where this field is always `null`) this field is always a real semver string here, never `null`. */
            serviceCore: string;
        };
        /** @description Administrator as `AdminService.view` projects it — never includes the password hash or the TOTP secret. */
        Admin: {
            /** Format: uuid */
            id: string;
            /** Format: email */
            email: string;
            name: string | null;
            /** @enum {string} */
            role: "admin" | "viewer";
            /** @enum {string} */
            status: "active" | "disabled";
            totpEnabled: boolean;
            /**
             * Format: date-time
             * @description Non-null only while a lockout is still in the future.
             */
            lockedUntil: string | null;
            /** Format: date-time */
            createdAt: string;
            /** Format: date-time */
            updatedAt: string;
            /** Format: date-time */
            lastLoginAt: string | null;
        };
        AdminSession: {
            id: string;
            /** @description True for the session making this request. */
            current: boolean;
            /** @description Epoch milliseconds (not an ISO string, unlike the timestamps on `Admin`). */
            createdAt: number;
            /** @description Epoch milliseconds. */
            lastSeenAt: number;
            ip: string | null;
            userAgent: string | null;
        };
        /** @description One entry of console's own local admin-activity log (distinct from the audit service). */
        AuditEntry: {
            id: number;
            /** Format: uuid */
            adminId: string | null;
            adminEmail: string | null;
            /** @description e.g. `login.succeeded`, `login.failed`, `password.changed`, `totp.enabled`, `admin.created`, `flags.flag.update`. */
            action: string;
            target: string | null;
            meta: {
                [key: string]: unknown;
            } | null;
            ip: string | null;
            /** Format: date-time */
            at: string;
        };
        /** @description A configured service instance as `ServiceRegistry#describe` projects it. API keys and metrics tokens are never included. */
        ServiceDescriptor: {
            /** @description The `sid` used on every proxy route. */
            id: string;
            /**
             * @description Decides which proxy routes are valid for this `sid`; a type mismatch is a `404 UNKNOWN_SERVICE`.
             * @enum {string}
             */
            type: "notify" | "auth" | "media" | "gateway" | "audit" | "shortlink" | "flags" | "scheduler" | "webhook-out" | "search" | "ratelimit" | "geo";
            label: string;
            /** @description Base URL console calls (server-side only). */
            url: string;
            publicUrl: string | null;
            /** @description False only for a gateway instance with no metrics token configured. */
            hasMetrics: boolean;
            polling: {
                enabled: boolean;
                intervalSec: number;
            };
        };
        /** @description Liveness/readiness probe result console collects itself by calling the service's own `/health` and `/ready` (3 s timeout, unauthenticated). A probe that throws is reported as `false`, never as an error response. */
        ServiceStatus: {
            health: boolean;
            ready: boolean;
            /** @description The parsed `/ready` body, or the first 200 characters of it when it is not JSON, or `null` when the probe failed outright. */
            readyDetail: unknown;
            latencyMs: number;
        };
        /** @description Headline numbers the typed client derives from the service's own Prometheus `/metrics`. Shape is per service type. `null` when the service exposes no metrics to console; `{ "error": "..." }` when collecting them failed — a failure here never fails the request. */
        ServiceSummary: {
            [key: string]: unknown;
        } | null;
        /** @description One service's registry entry plus its live probe result and metrics summary. */
        ServiceOverview: {
            id: string;
            type: string;
            label: string;
            url: string;
            health: boolean;
            ready: boolean;
            readyDetail: unknown;
            latencyMs: number;
            summary: components["schemas"]["ServiceSummary"];
        };
        /** @description One service's `GET /v1/info`, collected tolerantly: an unreachable service, one too old to serve the route (404), or a malformed body becomes `{ ok: false, error }` for that entry instead of failing the request. */
        ServiceAbout: {
            id: string;
            type: string;
            label: string;
            url: string;
            ok: boolean;
            /** @description Present when `ok` is true: that service's own `/v1/info` body. */
            data?: {
                [key: string]: unknown;
            };
            /** @description Present when `ok` is false. */
            error?: string;
        };
        /**
         * @description auth user detail, composed by console from three parallel downstream calls: `GET /v1/users/{id}`, `GET /v1/users/{id}/sessions` and `GET /v1/users/{id}/events?limit=50`.
         *
         *     Every other top-level field is auth's `GET /v1/users/{id}` body spread in unchanged (see that service's own OpenAPI document); the fields listed here are the ones console adds and therefore owns.
         */
        AuthUserDetail: {
            /** @description The `items` array of auth's sessions response (the envelope itself is dropped). */
            sessions?: {
                [key: string]: unknown;
            }[];
            /** @description The `items` array of auth's events response, first page of 50. */
            events?: {
                [key: string]: unknown;
            }[];
            /** @description The events response's `nextBefore` cursor, for `GET .../auth/users/{id}/events`. */
            eventsNextBefore?: string | null;
        } & {
            [key: string]: unknown;
        };
        /**
         * @description Feature-flag detail, composed by console from `GET /v1/flags/{key}` and `GET /v1/flags/{key}/history?limit=20` in parallel.
         *
         *     Every other top-level field is flags' `GET /v1/flags/{key}` body spread in unchanged (see that service's own OpenAPI document); the fields listed here are the ones console adds and therefore owns.
         */
        FlagDetail: {
            /** @description The `items` array of the flag's change history, first page of 20. */
            history?: {
                [key: string]: unknown;
            }[];
            /** @description The history response's `nextBefore` cursor, for `GET .../flags/flags/{id}/history`. */
            historyNextBefore?: string | null;
        } & {
            [key: string]: unknown;
        };
        /**
         * @description Scheduled-job detail, composed by console from `GET /v1/jobs/{name}` and `GET /v1/runs?job={name}&limit=20` in parallel.
         *
         *     Every other top-level field is scheduler's `GET /v1/jobs/{name}` body spread in unchanged (see that service's own OpenAPI document); the fields listed here are the ones console adds and therefore owns.
         */
        JobDetail: {
            /** @description The `items` array of the job's runs, first page of 20. */
            runs?: {
                [key: string]: unknown;
            }[];
            /** @description The runs response's `nextBefore` cursor, for `GET .../scheduler/jobs/{id}/runs`. */
            runsNextBefore?: string | null;
        } & {
            [key: string]: unknown;
        };
        /**
         * @description Webhook subscription detail, composed by console from `GET /v1/subscriptions/{id}` and `GET /v1/deliveries?subscription={id}&limit=20` in parallel.
         *
         *     Every other top-level field is webhook-out's `GET /v1/subscriptions/{id}` body spread in unchanged (see that service's own OpenAPI document); the fields listed here are the ones console adds and therefore owns.
         */
        SubscriptionDetail: {
            /** @description The `items` array of the subscription's deliveries, first page of 20. */
            deliveries?: {
                [key: string]: unknown;
            }[];
            /** @description The deliveries response's `nextBefore` cursor, for `GET .../webhook-out/deliveries?subscription=...`. */
            deliveriesNextBefore?: string | null;
        } & {
            [key: string]: unknown;
        };
        /**
         * @description Short-link detail, composed by console from `GET /v1/links/{code}` and `GET /v1/links/{code}/stats` in parallel.
         *
         *     Every other top-level field is shortlink's `GET /v1/links/{code}` body spread in unchanged (see that service's own OpenAPI document); the fields listed here are the ones console adds and therefore owns.
         */
        LinkDetail: {
            /** @description shortlink's whole `GET /v1/links/{code}/stats` body, nested unchanged under this key (honoring `days` from the query string when given). */
            stats?: {
                [key: string]: unknown;
            };
        } & {
            [key: string]: unknown;
        };
    };
    responses: {
        /** @description Request failed Ajv validation of its path, query or body schema. */
        ValidationFailed: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorEnvelope"];
            };
        };
        /** @description No usable session: the cookie is missing, expired or idle-timed-out (`UNAUTHENTICATED`), or the session exists but the second factor has not been completed yet (`TOTP_REQUIRED` — finish `POST /api/session/totp` first). */
        Unauthenticated: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorEnvelope"];
            };
        };
        /** @description Signed in, but not allowed: the `x-console-request: 1` header is missing on a mutating request, or the session's role is `viewer` where `admin` is required. */
        Forbidden: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorEnvelope"];
            };
        };
        /** @description Either the `sid` names no configured service, or it names one of the wrong type for this route — both are `404 UNKNOWN_SERVICE` with no `service` field — or the downstream service itself answered 404, in which case its own code and message are relayed and `service` names it. */
        ProxyNotFound: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["DownstreamErrorEnvelope"];
            };
        };
        /** @description The downstream service could not be reached at all — connection failure, or the `SERVICE_TIMEOUT_MS` deadline (10 s by default) elapsed. */
        Unreachable: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["DownstreamErrorEnvelope"];
            };
        };
        /** @description Any other status the downstream service returned, relayed with its own status code, `error.code`, `error.message` and `error.details`, plus `error.service`. console does not translate, normalize or re-map downstream error codes. */
        DownstreamDefault: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["DownstreamErrorEnvelope"];
            };
        };
        /** @description Resource not found. */
        NotFound: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorEnvelope"];
            };
        };
        /** @description The request conflicts with current state. */
        Conflict: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorEnvelope"];
            };
        };
        /** @description Too many attempts from this IP against the pre-session endpoints. console's in-process limiter allows `RATE_LIMIT_MAX` (default 30) attempts per IP per window and answers with `retry-after` plus `details.retryAfterSec`. */
        RateLimited: {
            headers: {
                "Retry-After"?: string;
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorEnvelope"];
            };
        };
    };
    parameters: {
        /** @description Page size. Forwarded downstream as a number. */
        Paging_limit: string;
        /** @description Opaque keyset cursor from the previous page. */
        Paging_cursor: string;
        Paging_status: string;
        Paging_email: string;
        /** @description Keyset bound for `before`-paginated downstream lists. */
        Paging_before: string;
        Paging_action: string;
        AuditQ_source: string;
        AuditQ_action: string;
        AuditQ_actionPrefix: string;
        AuditQ_outcome: "success" | "failure" | "denied";
        AuditQ_actorType: string;
        AuditQ_actorId: string;
        AuditQ_targetType: string;
        AuditQ_targetId: string;
        AuditQ_ip: string;
        AuditQ_requestId: string;
        AuditQ_from: string;
        AuditQ_to: string;
        AuditQ_limit: string;
        AuditQ_cursor: string;
        /** @description Export format; only read by the export operation. Default `ndjson`. */
        AuditQ_format: "ndjson" | "csv";
        AuditQ_hours: string;
        AuditQ_fromSeq: string;
        AuditQ_toSeq: string;
        ShortlinkQ_q: string;
        ShortlinkQ_tag: string;
        ShortlinkQ_status: "active" | "disabled" | "expired" | "exhausted";
        ShortlinkQ_createdBy: string;
        ShortlinkQ_limit: string;
        ShortlinkQ_cursor: string;
        /** @description Window, in days, for the stats aggregation. */
        ShortlinkQ_days: string;
        /** @description QR module scale; only read by the QR operation. */
        ShortlinkQ_scale: string;
        /** @description QR quiet-zone margin; only read by the QR operation. */
        ShortlinkQ_margin: string;
        FlagsQ_q: string;
        FlagsQ_tag: string;
        FlagsQ_kind: "boolean" | "string" | "number" | "json";
        FlagsQ_archived: "true" | "false";
        FlagsQ_limit: string;
        FlagsQ_cursor: string;
        FlagsQ_before: string;
        SchedulerQ_q: string;
        SchedulerQ_tag: string;
        SchedulerQ_enabled: "true" | "false";
        SchedulerQ_status: "pending" | "running" | "retrying" | "succeeded" | "failed" | "skipped" | "cancelled";
        SchedulerQ_job: string;
        SchedulerQ_limit: string;
        SchedulerQ_cursor: string;
        SchedulerQ_before: string;
        /** @description Cron expression to preview; only read by the preview operation. */
        SchedulerQ_cron: string;
        SchedulerQ_timezone: string;
        /** @description Number of upcoming fire times to preview. */
        SchedulerQ_count: string;
        WebhookQ_q: string;
        WebhookQ_status: "active" | "paused" | "disabled" | "pending" | "running" | "retrying" | "succeeded" | "failed" | "cancelled";
        WebhookQ_event: string;
        WebhookQ_type: string;
        WebhookQ_subscription: string;
        WebhookQ_limit: string;
        WebhookQ_cursor: string;
        WebhookQ_before: string;
        OffsetQ_limit: string;
        OffsetQ_offset: string;
        RlStatsQ_hours: string;
        RlTopQ_window: string;
        RlTopQ_limit: string;
        GeoIpQ_ip: string;
        /** @description BCP-47 language tag for localized names. */
        GeoIpQ_lang: string;
        GeoCountriesQ_q: string;
        GeoCountriesQ_continent: string;
        GeoCountriesQ_eu: "true" | "false";
        GeoCountriesQ_currency: string;
        GeoCountriesQ_lang: string;
        GeoTimezonesQ_country: string;
        GeoTimezonesQ_q: string;
        GeoTimezonesQ_lang: string;
        GeoPhoneQ_number: string;
        GeoPhoneQ_country: string;
        GeoDistanceQ_from: string;
        GeoDistanceQ_to: string;
        GeoNearbyQ_lat: string;
        GeoNearbyQ_lng: string;
        GeoNearbyQ_radius: string;
        GeoNearbyQ_limit: string;
        /** @description Signed-URL lifetime in seconds. */
        MediaTtlQ_ttl: string;
        /** @description Visibility for the stored file. */
        MediaUploadQ_visibility: "public" | "private";
        /** @description Stored file name; defaults to the one media derives itself. */
        MediaUploadQ_name: string;
        /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
        SidParam: string;
        /** @description Downstream resource id, passed through URL-encoded. */
        IdParam: string;
        /** @description Second-level downstream resource id (session, environment, document, override, subject, place or variant), passed through URL-encoded. */
        SubParam: string;
        /** @description Console administrator id (UUID). */
        AdminIdParam: string;
        /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
        CsrfHeader: "1";
    };
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    "console.health.get": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Process is alive. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Health"];
                };
            };
        };
    };
    "console.ready.get": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The database is reachable. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Health"];
                };
            };
            /** @description The database could not be pinged. */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ReadyUnavailable"];
                };
            };
        };
    };
    "console.info.get": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Service info. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Info"];
                };
            };
        };
    };
    "console.session.login": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    /** Format: email */
                    email: string;
                    /** @description Length is only shape-checked here (1-1024). Strength rules (at least 12 characters, no single repeated character, must not contain the local part of the account's email) are enforced in the domain layer and fail with `400 WEAK_PASSWORD` carrying `details.problems`. */
                    password: string;
                };
            };
        };
        responses: {
            /** @description Signed in, or signed in pending the second factor. The session cookie is set either way. */
            200: {
                headers: {
                    /** @description `console_session=<token>; Max-Age=<CONSOLE_SESSION_TTL_MIN*60>; Path=/; HttpOnly; SameSite=Strict[; Secure]`. */
                    "Set-Cookie"?: string;
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        totpRequired: boolean;
                        /** @description Null while a second factor is still pending. */
                        admin: components["schemas"]["Admin"] | null;
                    };
                };
            };
            400: components["responses"]["ValidationFailed"];
            /** @description `INVALID_CREDENTIALS` — unknown email or wrong password. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
            /** @description `ACCOUNT_DISABLED` — credentials were correct but the account is disabled. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
            /** @description `ACCOUNT_LOCKED` — too many recent failures; `details.retryAfterSec` says how long is left. */
            423: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
            429: components["responses"]["RateLimited"];
        };
    };
    "console.session.totp": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    /** @description Six-digit TOTP code. A code is single-use: the verified time step is claimed atomically, so replaying the same code within its window fails. */
                    code: string;
                };
            };
        };
        responses: {
            /** @description Second factor accepted; the session is now fully usable. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        admin: components["schemas"]["Admin"];
                    };
                };
            };
            400: components["responses"]["ValidationFailed"];
            /** @description `UNAUTHENTICATED` (no pending session) or `INVALID_TOTP` (wrong code, or no second factor pending). */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
            /** @description `ACCOUNT_LOCKED` — the failure threshold was reached; the pending session was deleted. */
            423: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
            429: components["responses"]["RateLimited"];
        };
    };
    "console.session.get": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Session state. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        admin: components["schemas"]["Admin"] | null;
                        totpPending: boolean;
                    };
                };
            };
        };
    };
    "console.session.logout": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Session revoked if there was one; the cookie is cleared either way. */
            204: {
                headers: {
                    /** @description `console_session=; Max-Age=0; Path=/; HttpOnly; SameSite=Strict[; Secure]`. */
                    "Set-Cookie"?: string;
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    "console.me.sessions.list": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Sessions belonging to the signed-in administrator, with the current one flagged. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        items: components["schemas"]["AdminSession"][];
                    };
                };
            };
            401: components["responses"]["Unauthenticated"];
        };
    };
    "console.me.sessions.logoutOthers": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Every session except the current one was revoked. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description How many sessions were removed. */
                        revoked: number;
                    };
                };
            };
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
        };
    };
    "console.me.password.change": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    /** @description Length is only shape-checked here (1-1024). Strength rules (at least 12 characters, no single repeated character, must not contain the local part of the account's email) are enforced in the domain layer and fail with `400 WEAK_PASSWORD` carrying `details.problems`. */
                    currentPassword: string;
                    /** @description Length is only shape-checked here (1-1024). Strength rules (at least 12 characters, no single repeated character, must not contain the local part of the account's email) are enforced in the domain layer and fail with `400 WEAK_PASSWORD` carrying `details.problems`. */
                    newPassword: string;
                };
            };
        };
        responses: {
            /** @description Password changed; all other sessions revoked. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description `VALIDATION_FAILED`, or `WEAK_PASSWORD` with `details.problems` listing every rule the new password broke. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
            /** @description `UNAUTHENTICATED`, or `INVALID_CREDENTIALS` when the current password is wrong. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
            403: components["responses"]["Forbidden"];
        };
    };
    "console.me.totp.start": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Enrolment started. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description Base32 TOTP secret. */
                        secret: string;
                        /** @description `otpauth://totp/...` URI for an authenticator app, using `TOTP_ISSUER`. */
                        uri: string;
                    };
                };
            };
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            /** @description `CONFLICT` — two-factor authentication is already enabled. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
            /** @description `TOTP_UNAVAILABLE` — `SECRETS_KEY` is not configured, so a secret cannot be sealed at rest and enrolment is refused. */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
        };
    };
    "console.me.totp.confirm": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    /** @description Six-digit TOTP code. A code is single-use: the verified time step is claimed atomically, so replaying the same code within its window fails. */
                    code: string;
                };
            };
        };
        responses: {
            /** @description Two-factor authentication is now enabled for this account. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["ValidationFailed"];
            /** @description `UNAUTHENTICATED`, or `INVALID_TOTP` when the code does not match or its time step was already used. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
            403: components["responses"]["Forbidden"];
            /** @description `CONFLICT` — no enrolment is in progress (never started, or already finished). */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
        };
    };
    "console.me.totp.disable": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    /** @description Length is only shape-checked here (1-1024). Strength rules (at least 12 characters, no single repeated character, must not contain the local part of the account's email) are enforced in the domain layer and fail with `400 WEAK_PASSWORD` carrying `details.problems`. */
                    password: string;
                    /** @description Six-digit TOTP code. A code is single-use: the verified time step is claimed atomically, so replaying the same code within its window fails. */
                    code: string;
                };
            };
        };
        responses: {
            /** @description Two-factor authentication disabled. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["ValidationFailed"];
            /** @description `UNAUTHENTICATED`, `INVALID_CREDENTIALS` (wrong password) or `INVALID_TOTP` (wrong code). */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
            403: components["responses"]["Forbidden"];
            /** @description `CONFLICT` — two-factor authentication is not enabled. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
            /** @description `TOTP_SECRET_CORRUPT` — the stored secret could not be unsealed with the configured keys. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
        };
    };
    "console.admins.list": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description All console accounts. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        items: components["schemas"]["Admin"][];
                    };
                };
            };
            401: components["responses"]["Unauthenticated"];
        };
    };
    "console.admins.create": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    /** Format: email */
                    email: string;
                    /** @description Length is only shape-checked here (1-1024). Strength rules (at least 12 characters, no single repeated character, must not contain the local part of the account's email) are enforced in the domain layer and fail with `400 WEAK_PASSWORD` carrying `details.problems`. */
                    password: string;
                    name?: string;
                    /** @enum {string} */
                    role: "admin" | "viewer";
                };
            };
        };
        responses: {
            /** @description Account created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        admin: components["schemas"]["Admin"];
                    };
                };
            };
            /** @description `VALIDATION_FAILED`, `INVALID_ARGUMENT` (email rejected by the domain-level check) or `WEAK_PASSWORD`. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            /** @description `EMAIL_TAKEN` — an account with this email already exists. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
        };
    };
    "console.admins.delete": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Console administrator id (UUID). */
                id: components["parameters"]["AdminIdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Account deleted and its sessions revoked. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            /** @description `NOT_FOUND` — no such administrator. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
            /** @description `CONFLICT` (you cannot delete your own account) or `LAST_ADMIN` (at least one active administrator must remain). */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
        };
    };
    "console.admins.update": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Console administrator id (UUID). */
                id: components["parameters"]["AdminIdParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    name?: string;
                    /** @enum {string} */
                    role?: "admin" | "viewer";
                    /** @enum {string} */
                    status?: "active" | "disabled";
                };
            };
        };
        responses: {
            /** @description Updated account. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        admin: components["schemas"]["Admin"];
                    };
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            /** @description `NOT_FOUND` — no such administrator. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
            /** @description `LAST_ADMIN` (at least one active administrator must remain) or `CONFLICT` (you cannot demote or disable your own account). */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
        };
    };
    "console.admins.resetPassword": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Console administrator id (UUID). */
                id: components["parameters"]["AdminIdParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    /** @description Length is only shape-checked here (1-1024). Strength rules (at least 12 characters, no single repeated character, must not contain the local part of the account's email) are enforced in the domain layer and fail with `400 WEAK_PASSWORD` carrying `details.problems`. */
                    password: string;
                };
            };
        };
        responses: {
            /** @description Password set; that administrator's sessions are revoked. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description `VALIDATION_FAILED` or `WEAK_PASSWORD`. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            /** @description `NOT_FOUND` — no such administrator. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
        };
    };
    "console.admins.unlock": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Console administrator id (UUID). */
                id: components["parameters"]["AdminIdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Lockout cleared. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            /** @description `NOT_FOUND` — no such administrator. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
        };
    };
    "console.activity.list": {
        parameters: {
            query?: {
                /** @description Page size. Forwarded downstream as a number. */
                limit?: components["parameters"]["Paging_limit"];
                /** @description Opaque keyset cursor from the previous page. */
                cursor?: components["parameters"]["Paging_cursor"];
                status?: components["parameters"]["Paging_status"];
                email?: components["parameters"]["Paging_email"];
                /** @description Keyset bound for `before`-paginated downstream lists. */
                before?: components["parameters"]["Paging_before"];
                action?: components["parameters"]["Paging_action"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of activity entries, newest first. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        items: components["schemas"]["AuditEntry"][];
                        /** @description Cursor for the next page, or null on the last page. */
                        nextBefore: string | null;
                    };
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
        };
    };
    "console.services.list": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Configured services. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        items: components["schemas"]["ServiceDescriptor"][];
                    };
                };
            };
            401: components["responses"]["Unauthenticated"];
        };
    };
    "console.services.overview": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Live status for every configured service. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        items: components["schemas"]["ServiceOverview"][];
                    };
                };
            };
            401: components["responses"]["Unauthenticated"];
        };
    };
    "console.services.about": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Per-service identity, version and capabilities. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        items: components["schemas"]["ServiceAbout"][];
                    };
                };
            };
            401: components["responses"]["Unauthenticated"];
        };
    };
    "console.services.updateSettings": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    polling: {
                        enabled: boolean;
                        intervalSec: number;
                    };
                };
            };
        };
        responses: {
            /** @description Updated registry entry. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        service?: components["schemas"]["ServiceDescriptor"];
                    };
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            /** @description `NOT_FOUND` — no service with this id is configured. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorEnvelope"];
                };
            };
        };
    };
    "console.services.status": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Probe result plus the metrics summary. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ServiceStatus"] & {
                        summary: components["schemas"]["ServiceSummary"];
                    };
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
        };
    };
    "console.notify.messages.list": {
        parameters: {
            query?: {
                /** @description Page size. Forwarded downstream as a number. */
                limit?: components["parameters"]["Paging_limit"];
                /** @description Opaque keyset cursor from the previous page. */
                cursor?: components["parameters"]["Paging_cursor"];
                status?: components["parameters"]["Paging_status"];
                email?: components["parameters"]["Paging_email"];
                /** @description Keyset bound for `before`-paginated downstream lists. */
                before?: components["parameters"]["Paging_before"];
                action?: components["parameters"]["Paging_action"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the notify service's own `GET /v1/messages` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the notify service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.notify.messages.send": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        /** @description One of the two request bodies console does NOT constrain: the Ajv schema is exactly `{ type: "object" }`, so the body is forwarded to notify verbatim and notify's own schema is the only thing that validates it. `channel`, `template` and `to` are read (for the activity-log entry) but not required here. */
        requestBody: {
            content: {
                "application/json": {
                    [key: string]: unknown;
                };
            };
        };
        responses: {
            /** @description Accepted; the body is notify's own send response. console declares no response schema for this route, so no precise shape is asserted here — see the notify service's own OpenAPI document for the authoritative body. */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.notify.messages.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the notify service's own `GET /v1/messages/{id}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the notify service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.notify.messages.retry": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the notify service's own `POST /v1/messages/{id}/retry` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the notify service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.notify.templates.list": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the notify service's own `GET /v1/templates` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the notify service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.auth.users.list": {
        parameters: {
            query?: {
                /** @description Page size. Forwarded downstream as a number. */
                limit?: components["parameters"]["Paging_limit"];
                /** @description Opaque keyset cursor from the previous page. */
                cursor?: components["parameters"]["Paging_cursor"];
                status?: components["parameters"]["Paging_status"];
                email?: components["parameters"]["Paging_email"];
                /** @description Keyset bound for `before`-paginated downstream lists. */
                before?: components["parameters"]["Paging_before"];
                action?: components["parameters"]["Paging_action"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the auth service's own `GET /v1/users` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the auth service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.auth.users.create": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    /** Format: email */
                    email: string;
                    password: string;
                    name?: string;
                };
            };
        };
        responses: {
            /** @description Created; the body is auth's own create response. console declares no response schema for this route, so no precise shape is asserted here — see the auth service's own OpenAPI document for the authoritative body. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.auth.users.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Composed by console from more than one downstream call — this shape is console's own contract, not a relay of any single auth response. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthUserDetail"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.auth.users.delete": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. console discards auth's response body. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.auth.users.update": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    name?: string | null;
                    /** @enum {string} */
                    status?: "active" | "disabled";
                };
            };
        };
        responses: {
            /** @description Pass-through of the auth service's own `PATCH /v1/users/{id}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the auth service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.auth.users.events": {
        parameters: {
            query?: {
                /** @description Page size. Forwarded downstream as a number. */
                limit?: components["parameters"]["Paging_limit"];
                /** @description Opaque keyset cursor from the previous page. */
                cursor?: components["parameters"]["Paging_cursor"];
                status?: components["parameters"]["Paging_status"];
                email?: components["parameters"]["Paging_email"];
                /** @description Keyset bound for `before`-paginated downstream lists. */
                before?: components["parameters"]["Paging_before"];
                action?: components["parameters"]["Paging_action"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the auth service's own `GET /v1/users/{id}/events` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the auth service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.auth.users.revokeAllSessions": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the auth service's own `DELETE /v1/users/{id}/sessions` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the auth service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.auth.users.revokeSession": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
                /** @description Second-level downstream resource id (session, environment, document, override, subject, place or variant), passed through URL-encoded. */
                sub: components["parameters"]["SubParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Revoked. console discards auth's response body. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.auth.users.resendVerification": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        /** @description The email is what auth acts on; the `{id}` in the path is used only for console's own activity-log entry and is never sent downstream. */
        requestBody: {
            content: {
                "application/json": {
                    /** Format: email */
                    email: string;
                };
            };
        };
        responses: {
            /** @description console's own fixed acknowledgement — auth's response body is discarded. */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        accepted: true;
                    };
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.auth.users.passwordResetEmail": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        /** @description As with resend-verification, the email is the downstream input and `{id}` only labels the activity-log entry. */
        requestBody: {
            content: {
                "application/json": {
                    /** Format: email */
                    email: string;
                };
            };
        };
        responses: {
            /** @description console's own fixed acknowledgement — auth's response body is discarded. */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        accepted: true;
                    };
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.auth.jwks.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the auth service's own `GET /.well-known/jwks.json` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the auth service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.media.files.list": {
        parameters: {
            query?: {
                /** @description Page size. Forwarded downstream as a number. */
                limit?: components["parameters"]["Paging_limit"];
                /** @description Opaque keyset cursor from the previous page. */
                cursor?: components["parameters"]["Paging_cursor"];
                status?: components["parameters"]["Paging_status"];
                email?: components["parameters"]["Paging_email"];
                /** @description Keyset bound for `before`-paginated downstream lists. */
                before?: components["parameters"]["Paging_before"];
                action?: components["parameters"]["Paging_action"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the media service's own `GET /v1/files` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the media service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.media.files.uploadTicketed": {
        parameters: {
            query?: {
                /** @description Visibility for the stored file. */
                visibility?: components["parameters"]["MediaUploadQ_visibility"];
                /** @description Stored file name; defaults to the one media derives itself. */
                name?: components["parameters"]["MediaUploadQ_name"];
            };
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        /** @description Raw file bytes. Any content type is accepted (a wildcard parser hands the unbuffered stream to the media client); the request's own `content-type` and `content-length` are relayed downstream. Hard limit 512 MiB. */
        requestBody: {
            content: {
                "*/*": string;
            };
        };
        responses: {
            /** @description Created; the body is media's own upload response. console declares no response schema for this route, so no precise shape is asserted here — see the media service's own OpenAPI document for the authoritative body. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.media.files.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the media service's own `GET /v1/files/{id}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the media service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.media.files.delete": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. console discards media's response body. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.media.files.update": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    name?: string;
                    /** @enum {string} */
                    visibility?: "public" | "private";
                };
            };
        };
        responses: {
            /** @description Pass-through of the media service's own `PATCH /v1/files/{id}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the media service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.media.files.bytes": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
                /** @description Second-level downstream resource id (session, environment, document, override, subject, place or variant), passed through URL-encoded. */
                sub: components["parameters"]["SubParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The file bytes. Content type is the downstream one for the five allowed raster types, `application/octet-stream` for everything else. */
            200: {
                headers: {
                    "Content-Disposition"?: string;
                    /** @description Always `default-src 'none'; sandbox`. */
                    "Content-Security-Policy"?: string;
                    /** @description Relayed when the downstream response had one. */
                    ETag?: string;
                    [name: string]: unknown;
                };
                content: {
                    "application/octet-stream": string;
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.media.files.restore": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the media service's own `POST /v1/files/{id}/restore` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the media service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.media.files.urls": {
        parameters: {
            query?: {
                /** @description Signed-URL lifetime in seconds. */
                ttl?: components["parameters"]["MediaTtlQ_ttl"];
            };
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the media service's own `POST /v1/files/{id}/urls` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the media service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.media.tickets.create": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        /** @description The second unconstrained body: the Ajv schema is exactly `{ type: "object" }`, so it is forwarded to media verbatim and media's own schema is the only validation. */
        requestBody: {
            content: {
                "application/json": {
                    [key: string]: unknown;
                };
            };
        };
        responses: {
            /** @description Created; the body is media's own ticket response. console declares no response schema for this route, so no precise shape is asserted here — see the media service's own OpenAPI document for the authoritative body. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.audit.events.list": {
        parameters: {
            query?: {
                source?: components["parameters"]["AuditQ_source"];
                action?: components["parameters"]["AuditQ_action"];
                actionPrefix?: components["parameters"]["AuditQ_actionPrefix"];
                outcome?: components["parameters"]["AuditQ_outcome"];
                actorType?: components["parameters"]["AuditQ_actorType"];
                actorId?: components["parameters"]["AuditQ_actorId"];
                targetType?: components["parameters"]["AuditQ_targetType"];
                targetId?: components["parameters"]["AuditQ_targetId"];
                ip?: components["parameters"]["AuditQ_ip"];
                requestId?: components["parameters"]["AuditQ_requestId"];
                from?: components["parameters"]["AuditQ_from"];
                to?: components["parameters"]["AuditQ_to"];
                limit?: components["parameters"]["AuditQ_limit"];
                cursor?: components["parameters"]["AuditQ_cursor"];
                /** @description Export format; only read by the export operation. Default `ndjson`. */
                format?: components["parameters"]["AuditQ_format"];
                hours?: components["parameters"]["AuditQ_hours"];
                fromSeq?: components["parameters"]["AuditQ_fromSeq"];
                toSeq?: components["parameters"]["AuditQ_toSeq"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the audit service's own `GET /v1/events` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the audit service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.audit.events.export": {
        parameters: {
            query?: {
                source?: components["parameters"]["AuditQ_source"];
                action?: components["parameters"]["AuditQ_action"];
                actionPrefix?: components["parameters"]["AuditQ_actionPrefix"];
                outcome?: components["parameters"]["AuditQ_outcome"];
                actorType?: components["parameters"]["AuditQ_actorType"];
                actorId?: components["parameters"]["AuditQ_actorId"];
                targetType?: components["parameters"]["AuditQ_targetType"];
                targetId?: components["parameters"]["AuditQ_targetId"];
                ip?: components["parameters"]["AuditQ_ip"];
                requestId?: components["parameters"]["AuditQ_requestId"];
                from?: components["parameters"]["AuditQ_from"];
                to?: components["parameters"]["AuditQ_to"];
                limit?: components["parameters"]["AuditQ_limit"];
                cursor?: components["parameters"]["AuditQ_cursor"];
                /** @description Export format; only read by the export operation. Default `ndjson`. */
                format?: components["parameters"]["AuditQ_format"];
                hours?: components["parameters"]["AuditQ_hours"];
                fromSeq?: components["parameters"]["AuditQ_fromSeq"];
                toSeq?: components["parameters"]["AuditQ_toSeq"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The export stream — `application/x-ndjson` or `text/csv`, as the audit service labelled it. */
            200: {
                headers: {
                    /** @description Relayed, or `attachment; filename="audit.<format>"` when the downstream sent none. */
                    "Content-Disposition"?: string;
                    /** @description Always `default-src 'none'; sandbox`. */
                    "Content-Security-Policy"?: string;
                    [name: string]: unknown;
                };
                content: {
                    "application/x-ndjson": string;
                    "text/csv": string;
                    "application/octet-stream": string;
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.audit.events.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the audit service's own `GET /v1/events/{id}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the audit service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.audit.stats.get": {
        parameters: {
            query?: {
                source?: components["parameters"]["AuditQ_source"];
                action?: components["parameters"]["AuditQ_action"];
                actionPrefix?: components["parameters"]["AuditQ_actionPrefix"];
                outcome?: components["parameters"]["AuditQ_outcome"];
                actorType?: components["parameters"]["AuditQ_actorType"];
                actorId?: components["parameters"]["AuditQ_actorId"];
                targetType?: components["parameters"]["AuditQ_targetType"];
                targetId?: components["parameters"]["AuditQ_targetId"];
                ip?: components["parameters"]["AuditQ_ip"];
                requestId?: components["parameters"]["AuditQ_requestId"];
                from?: components["parameters"]["AuditQ_from"];
                to?: components["parameters"]["AuditQ_to"];
                limit?: components["parameters"]["AuditQ_limit"];
                cursor?: components["parameters"]["AuditQ_cursor"];
                /** @description Export format; only read by the export operation. Default `ndjson`. */
                format?: components["parameters"]["AuditQ_format"];
                hours?: components["parameters"]["AuditQ_hours"];
                fromSeq?: components["parameters"]["AuditQ_fromSeq"];
                toSeq?: components["parameters"]["AuditQ_toSeq"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the audit service's own `GET /v1/stats` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the audit service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.audit.chain.head": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the audit service's own `GET /v1/chain/head` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the audit service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.audit.chain.verify": {
        parameters: {
            query?: {
                source?: components["parameters"]["AuditQ_source"];
                action?: components["parameters"]["AuditQ_action"];
                actionPrefix?: components["parameters"]["AuditQ_actionPrefix"];
                outcome?: components["parameters"]["AuditQ_outcome"];
                actorType?: components["parameters"]["AuditQ_actorType"];
                actorId?: components["parameters"]["AuditQ_actorId"];
                targetType?: components["parameters"]["AuditQ_targetType"];
                targetId?: components["parameters"]["AuditQ_targetId"];
                ip?: components["parameters"]["AuditQ_ip"];
                requestId?: components["parameters"]["AuditQ_requestId"];
                from?: components["parameters"]["AuditQ_from"];
                to?: components["parameters"]["AuditQ_to"];
                limit?: components["parameters"]["AuditQ_limit"];
                cursor?: components["parameters"]["AuditQ_cursor"];
                /** @description Export format; only read by the export operation. Default `ndjson`. */
                format?: components["parameters"]["AuditQ_format"];
                hours?: components["parameters"]["AuditQ_hours"];
                fromSeq?: components["parameters"]["AuditQ_fromSeq"];
                toSeq?: components["parameters"]["AuditQ_toSeq"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the audit service's own `GET /v1/chain/verify` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the audit service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.shortlink.links.list": {
        parameters: {
            query?: {
                q?: components["parameters"]["ShortlinkQ_q"];
                tag?: components["parameters"]["ShortlinkQ_tag"];
                status?: components["parameters"]["ShortlinkQ_status"];
                createdBy?: components["parameters"]["ShortlinkQ_createdBy"];
                limit?: components["parameters"]["ShortlinkQ_limit"];
                cursor?: components["parameters"]["ShortlinkQ_cursor"];
                /** @description Window, in days, for the stats aggregation. */
                days?: components["parameters"]["ShortlinkQ_days"];
                /** @description QR module scale; only read by the QR operation. */
                scale?: components["parameters"]["ShortlinkQ_scale"];
                /** @description QR quiet-zone margin; only read by the QR operation. */
                margin?: components["parameters"]["ShortlinkQ_margin"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the shortlink service's own `GET /v1/links` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the shortlink service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.shortlink.links.create": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    url: string;
                    slug?: string;
                    permanent?: boolean;
                    expiresAt?: string | null;
                    maxClicks?: number | null;
                    tags?: string[];
                    note?: string | null;
                };
            };
        };
        responses: {
            /** @description Created; the body is shortlink's own create response. console declares no response schema for this route, so no precise shape is asserted here — see the shortlink service's own OpenAPI document for the authoritative body. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.shortlink.links.get": {
        parameters: {
            query?: {
                q?: components["parameters"]["ShortlinkQ_q"];
                tag?: components["parameters"]["ShortlinkQ_tag"];
                status?: components["parameters"]["ShortlinkQ_status"];
                createdBy?: components["parameters"]["ShortlinkQ_createdBy"];
                limit?: components["parameters"]["ShortlinkQ_limit"];
                cursor?: components["parameters"]["ShortlinkQ_cursor"];
                /** @description Window, in days, for the stats aggregation. */
                days?: components["parameters"]["ShortlinkQ_days"];
                /** @description QR module scale; only read by the QR operation. */
                scale?: components["parameters"]["ShortlinkQ_scale"];
                /** @description QR quiet-zone margin; only read by the QR operation. */
                margin?: components["parameters"]["ShortlinkQ_margin"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Composed by console from more than one downstream call — this shape is console's own contract, not a relay of any single shortlink response. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["LinkDetail"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.shortlink.links.delete": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. console discards shortlink's response body. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.shortlink.links.update": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    url?: string;
                    permanent?: boolean;
                    enabled?: boolean;
                    expiresAt?: string | null;
                    maxClicks?: number | null;
                    tags?: string[];
                    note?: string | null;
                };
            };
        };
        responses: {
            /** @description Pass-through of the shortlink service's own `PATCH /v1/links/{code}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the shortlink service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.shortlink.links.stats": {
        parameters: {
            query?: {
                q?: components["parameters"]["ShortlinkQ_q"];
                tag?: components["parameters"]["ShortlinkQ_tag"];
                status?: components["parameters"]["ShortlinkQ_status"];
                createdBy?: components["parameters"]["ShortlinkQ_createdBy"];
                limit?: components["parameters"]["ShortlinkQ_limit"];
                cursor?: components["parameters"]["ShortlinkQ_cursor"];
                /** @description Window, in days, for the stats aggregation. */
                days?: components["parameters"]["ShortlinkQ_days"];
                /** @description QR module scale; only read by the QR operation. */
                scale?: components["parameters"]["ShortlinkQ_scale"];
                /** @description QR quiet-zone margin; only read by the QR operation. */
                margin?: components["parameters"]["ShortlinkQ_margin"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the shortlink service's own `GET /v1/links/{code}/stats` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the shortlink service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.shortlink.links.qr": {
        parameters: {
            query?: {
                q?: components["parameters"]["ShortlinkQ_q"];
                tag?: components["parameters"]["ShortlinkQ_tag"];
                status?: components["parameters"]["ShortlinkQ_status"];
                createdBy?: components["parameters"]["ShortlinkQ_createdBy"];
                limit?: components["parameters"]["ShortlinkQ_limit"];
                cursor?: components["parameters"]["ShortlinkQ_cursor"];
                /** @description Window, in days, for the stats aggregation. */
                days?: components["parameters"]["ShortlinkQ_days"];
                /** @description QR module scale; only read by the QR operation. */
                scale?: components["parameters"]["ShortlinkQ_scale"];
                /** @description QR quiet-zone margin; only read by the QR operation. */
                margin?: components["parameters"]["ShortlinkQ_margin"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The QR code image. */
            200: {
                headers: {
                    /** @description `inline; filename="{id}.png"`. */
                    "Content-Disposition"?: string;
                    /** @description Always `default-src 'none'; sandbox`. */
                    "Content-Security-Policy"?: string;
                    [name: string]: unknown;
                };
                content: {
                    "image/png": string;
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.shortlink.stats.get": {
        parameters: {
            query?: {
                q?: components["parameters"]["ShortlinkQ_q"];
                tag?: components["parameters"]["ShortlinkQ_tag"];
                status?: components["parameters"]["ShortlinkQ_status"];
                createdBy?: components["parameters"]["ShortlinkQ_createdBy"];
                limit?: components["parameters"]["ShortlinkQ_limit"];
                cursor?: components["parameters"]["ShortlinkQ_cursor"];
                /** @description Window, in days, for the stats aggregation. */
                days?: components["parameters"]["ShortlinkQ_days"];
                /** @description QR module scale; only read by the QR operation. */
                scale?: components["parameters"]["ShortlinkQ_scale"];
                /** @description QR quiet-zone margin; only read by the QR operation. */
                margin?: components["parameters"]["ShortlinkQ_margin"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the shortlink service's own `GET /v1/stats` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the shortlink service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.flags.environments.list": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the flags service's own `GET /v1/environments` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the flags service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.flags.stats.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the flags service's own `GET /v1/stats` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the flags service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.flags.flags.list": {
        parameters: {
            query?: {
                q?: components["parameters"]["FlagsQ_q"];
                tag?: components["parameters"]["FlagsQ_tag"];
                kind?: components["parameters"]["FlagsQ_kind"];
                archived?: components["parameters"]["FlagsQ_archived"];
                limit?: components["parameters"]["FlagsQ_limit"];
                cursor?: components["parameters"]["FlagsQ_cursor"];
                before?: components["parameters"]["FlagsQ_before"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the flags service's own `GET /v1/flags` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the flags service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.flags.flags.create": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    key: string;
                    /** @enum {string} */
                    kind: "boolean" | "string" | "number" | "json";
                    description?: string;
                    tags?: string[];
                    /** @description Any JSON value; console does not constrain it — the flag's `kind` is what flags itself validates it against. */
                    value?: unknown;
                    /** @description Any JSON value, as for `value`. */
                    offValue?: unknown;
                    enabled?: boolean;
                };
            };
        };
        responses: {
            /** @description Created; the body is flags' own create response. console declares no response schema for this route, so no precise shape is asserted here — see the flags service's own OpenAPI document for the authoritative body. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.flags.flags.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Composed by console from more than one downstream call — this shape is console's own contract, not a relay of any single flags response. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["FlagDetail"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.flags.flags.delete": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. console discards flags' response body. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.flags.flags.update": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    description?: string;
                    tags?: string[];
                    archived?: boolean;
                    /** @description Re-randomizes percentage-rollout bucketing downstream. */
                    reshuffle?: boolean;
                };
            };
        };
        responses: {
            /** @description Pass-through of the flags service's own `PATCH /v1/flags/{key}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the flags service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.flags.flags.history": {
        parameters: {
            query?: {
                q?: components["parameters"]["FlagsQ_q"];
                tag?: components["parameters"]["FlagsQ_tag"];
                kind?: components["parameters"]["FlagsQ_kind"];
                archived?: components["parameters"]["FlagsQ_archived"];
                limit?: components["parameters"]["FlagsQ_limit"];
                cursor?: components["parameters"]["FlagsQ_cursor"];
                before?: components["parameters"]["FlagsQ_before"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the flags service's own `GET /v1/flags/{key}/history` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the flags service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.flags.history.list": {
        parameters: {
            query?: {
                q?: components["parameters"]["FlagsQ_q"];
                tag?: components["parameters"]["FlagsQ_tag"];
                kind?: components["parameters"]["FlagsQ_kind"];
                archived?: components["parameters"]["FlagsQ_archived"];
                limit?: components["parameters"]["FlagsQ_limit"];
                cursor?: components["parameters"]["FlagsQ_cursor"];
                before?: components["parameters"]["FlagsQ_before"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the flags service's own `GET /v1/history` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the flags service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.flags.envs.update": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
                /** @description Second-level downstream resource id (session, environment, document, override, subject, place or variant), passed through URL-encoded. */
                sub: components["parameters"]["SubParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    enabled?: boolean;
                    /** @description Any JSON value; validated downstream against the flag's `kind`. */
                    value?: unknown;
                    /** @description Any JSON value. */
                    offValue?: unknown;
                    percentage?: number;
                    /** @description Targeting rules. console checks only that this is an array of at most 100 entries; their shape is flags' contract, not console's. */
                    rules?: unknown[];
                };
            };
        };
        responses: {
            /** @description Pass-through of the flags service's own `PATCH /v1/flags/{key}/envs/{env}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the flags service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.flags.envs.copy": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
                /** @description Second-level downstream resource id (session, environment, document, override, subject, place or variant), passed through URL-encoded. */
                sub: components["parameters"]["SubParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    /** @description Destination environment key. */
                    to: string;
                };
            };
        };
        responses: {
            /** @description Pass-through of the flags service's own `POST /v1/flags/{key}/envs/{env}/copy` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the flags service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.flags.evaluate.post": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    env: string;
                    context?: {
                        userId?: string;
                        email?: string;
                        attrs?: {
                            [key: string]: string;
                        };
                    };
                    /** @description Restrict evaluation to these flag keys; omit for all. */
                    keys?: string[];
                };
            };
        };
        responses: {
            /** @description Pass-through of the flags service's own `POST /v1/evaluate` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the flags service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.scheduler.stats.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the scheduler service's own `GET /v1/stats` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the scheduler service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.scheduler.targetKeys.list": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the scheduler service's own `GET /v1/target-keys` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the scheduler service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.scheduler.timezones.list": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the scheduler service's own `GET /v1/timezones` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the scheduler service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.scheduler.preview.get": {
        parameters: {
            query?: {
                q?: components["parameters"]["SchedulerQ_q"];
                tag?: components["parameters"]["SchedulerQ_tag"];
                enabled?: components["parameters"]["SchedulerQ_enabled"];
                status?: components["parameters"]["SchedulerQ_status"];
                job?: components["parameters"]["SchedulerQ_job"];
                limit?: components["parameters"]["SchedulerQ_limit"];
                cursor?: components["parameters"]["SchedulerQ_cursor"];
                before?: components["parameters"]["SchedulerQ_before"];
                /** @description Cron expression to preview; only read by the preview operation. */
                cron?: components["parameters"]["SchedulerQ_cron"];
                timezone?: components["parameters"]["SchedulerQ_timezone"];
                /** @description Number of upcoming fire times to preview. */
                count?: components["parameters"]["SchedulerQ_count"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the scheduler service's own `GET /v1/preview` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the scheduler service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.scheduler.jobs.list": {
        parameters: {
            query?: {
                q?: components["parameters"]["SchedulerQ_q"];
                tag?: components["parameters"]["SchedulerQ_tag"];
                enabled?: components["parameters"]["SchedulerQ_enabled"];
                status?: components["parameters"]["SchedulerQ_status"];
                job?: components["parameters"]["SchedulerQ_job"];
                limit?: components["parameters"]["SchedulerQ_limit"];
                cursor?: components["parameters"]["SchedulerQ_cursor"];
                before?: components["parameters"]["SchedulerQ_before"];
                /** @description Cron expression to preview; only read by the preview operation. */
                cron?: components["parameters"]["SchedulerQ_cron"];
                timezone?: components["parameters"]["SchedulerQ_timezone"];
                /** @description Number of upcoming fire times to preview. */
                count?: components["parameters"]["SchedulerQ_count"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the scheduler service's own `GET /v1/jobs` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the scheduler service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.scheduler.jobs.create": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    name: string;
                    description?: string;
                    tags?: string[];
                    enabled?: boolean;
                    /** @description console requires the object but leaves the cron-vs-at choice to scheduler. */
                    schedule: {
                        cron?: string;
                        timezone?: string;
                        /** @description One-shot ISO timestamp, as an alternative to `cron`. */
                        at?: string;
                    };
                    target: {
                        url: string;
                        /** @enum {string} */
                        method?: "GET" | "POST" | "PUT" | "PATCH" | "DELETE";
                        headers?: {
                            [key: string]: string;
                        };
                        /** @description Any JSON value, sent as the job's request body. */
                        body?: unknown;
                    };
                    targetKey?: string | null;
                    timeoutMs?: number;
                    retry?: {
                        max?: number;
                        backoffSec?: number;
                    };
                };
            };
        };
        responses: {
            /** @description Created; the body is scheduler's own create response. console declares no response schema for this route, so no precise shape is asserted here — see the scheduler service's own OpenAPI document for the authoritative body. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.scheduler.jobs.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Composed by console from more than one downstream call — this shape is console's own contract, not a relay of any single scheduler response. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JobDetail"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.scheduler.jobs.delete": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. console discards scheduler's response body. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.scheduler.jobs.update": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    description?: string;
                    tags?: string[];
                    enabled?: boolean;
                    /** @description console requires the object but leaves the cron-vs-at choice to scheduler. */
                    schedule?: {
                        cron?: string;
                        timezone?: string;
                        /** @description One-shot ISO timestamp, as an alternative to `cron`. */
                        at?: string;
                    };
                    target?: {
                        url: string;
                        /** @enum {string} */
                        method?: "GET" | "POST" | "PUT" | "PATCH" | "DELETE";
                        headers?: {
                            [key: string]: string;
                        };
                        /** @description Any JSON value, sent as the job's request body. */
                        body?: unknown;
                    };
                    targetKey?: string | null;
                    timeoutMs?: number;
                    retry?: {
                        max?: number;
                        backoffSec?: number;
                    };
                };
            };
        };
        responses: {
            /** @description Pass-through of the scheduler service's own `PATCH /v1/jobs/{name}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the scheduler service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.scheduler.jobs.run": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Accepted; the body is scheduler's own run response. console declares no response schema for this route, so no precise shape is asserted here — see the scheduler service's own OpenAPI document for the authoritative body. */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.scheduler.jobs.runs": {
        parameters: {
            query?: {
                q?: components["parameters"]["SchedulerQ_q"];
                tag?: components["parameters"]["SchedulerQ_tag"];
                enabled?: components["parameters"]["SchedulerQ_enabled"];
                status?: components["parameters"]["SchedulerQ_status"];
                job?: components["parameters"]["SchedulerQ_job"];
                limit?: components["parameters"]["SchedulerQ_limit"];
                cursor?: components["parameters"]["SchedulerQ_cursor"];
                before?: components["parameters"]["SchedulerQ_before"];
                /** @description Cron expression to preview; only read by the preview operation. */
                cron?: components["parameters"]["SchedulerQ_cron"];
                timezone?: components["parameters"]["SchedulerQ_timezone"];
                /** @description Number of upcoming fire times to preview. */
                count?: components["parameters"]["SchedulerQ_count"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the scheduler service's own `GET /v1/runs?job={name}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the scheduler service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.scheduler.runs.list": {
        parameters: {
            query?: {
                q?: components["parameters"]["SchedulerQ_q"];
                tag?: components["parameters"]["SchedulerQ_tag"];
                enabled?: components["parameters"]["SchedulerQ_enabled"];
                status?: components["parameters"]["SchedulerQ_status"];
                job?: components["parameters"]["SchedulerQ_job"];
                limit?: components["parameters"]["SchedulerQ_limit"];
                cursor?: components["parameters"]["SchedulerQ_cursor"];
                before?: components["parameters"]["SchedulerQ_before"];
                /** @description Cron expression to preview; only read by the preview operation. */
                cron?: components["parameters"]["SchedulerQ_cron"];
                timezone?: components["parameters"]["SchedulerQ_timezone"];
                /** @description Number of upcoming fire times to preview. */
                count?: components["parameters"]["SchedulerQ_count"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the scheduler service's own `GET /v1/runs` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the scheduler service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.scheduler.runs.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the scheduler service's own `GET /v1/runs/{id}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the scheduler service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.scheduler.runs.cancel": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the scheduler service's own `POST /v1/runs/{id}/cancel` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the scheduler service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.webhookOut.stats.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the webhook-out service's own `GET /v1/stats` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the webhook-out service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.webhookOut.eventTypes.list": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the webhook-out service's own `GET /v1/event-types` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the webhook-out service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.webhookOut.subscriptions.list": {
        parameters: {
            query?: {
                q?: components["parameters"]["WebhookQ_q"];
                status?: components["parameters"]["WebhookQ_status"];
                event?: components["parameters"]["WebhookQ_event"];
                type?: components["parameters"]["WebhookQ_type"];
                subscription?: components["parameters"]["WebhookQ_subscription"];
                limit?: components["parameters"]["WebhookQ_limit"];
                cursor?: components["parameters"]["WebhookQ_cursor"];
                before?: components["parameters"]["WebhookQ_before"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the webhook-out service's own `GET /v1/subscriptions` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the webhook-out service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.webhookOut.subscriptions.create": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    name: string;
                    url: string;
                    events: string[];
                    description?: string;
                    headers?: {
                        [key: string]: string;
                    };
                    enabled?: boolean;
                };
            };
        };
        responses: {
            /** @description Created; the body is webhook-out's own create response, including the signing secret it mints. console declares no response schema for this route, so no precise shape is asserted here — see the webhook-out service's own OpenAPI document for the authoritative body. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.webhookOut.subscriptions.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Composed by console from more than one downstream call — this shape is console's own contract, not a relay of any single webhook-out response. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SubscriptionDetail"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.webhookOut.subscriptions.delete": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. console discards webhook-out's response body. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.webhookOut.subscriptions.update": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    name?: string;
                    url?: string;
                    events?: string[];
                    description?: string;
                    headers?: {
                        [key: string]: string;
                    };
                    enabled?: boolean;
                };
            };
        };
        responses: {
            /** @description Pass-through of the webhook-out service's own `PATCH /v1/subscriptions/{id}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the webhook-out service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.webhookOut.subscriptions.rotate": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the webhook-out service's own `POST /v1/subscriptions/{id}/rotate` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the webhook-out service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.webhookOut.subscriptions.test": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Accepted; the body is webhook-out's own test response. console declares no response schema for this route, so no precise shape is asserted here — see the webhook-out service's own OpenAPI document for the authoritative body. */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.webhookOut.subscriptions.replay": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    /** @description Inclusive ISO start of the range. */
                    from: string;
                    /** @description End of the range; webhook-out decides the default when omitted. */
                    to?: string;
                };
            };
        };
        responses: {
            /** @description Accepted; the body is webhook-out's own replay response. console declares no response schema for this route, so no precise shape is asserted here — see the webhook-out service's own OpenAPI document for the authoritative body. */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.webhookOut.deliveries.list": {
        parameters: {
            query?: {
                q?: components["parameters"]["WebhookQ_q"];
                status?: components["parameters"]["WebhookQ_status"];
                event?: components["parameters"]["WebhookQ_event"];
                type?: components["parameters"]["WebhookQ_type"];
                subscription?: components["parameters"]["WebhookQ_subscription"];
                limit?: components["parameters"]["WebhookQ_limit"];
                cursor?: components["parameters"]["WebhookQ_cursor"];
                before?: components["parameters"]["WebhookQ_before"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the webhook-out service's own `GET /v1/deliveries` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the webhook-out service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.webhookOut.deliveries.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the webhook-out service's own `GET /v1/deliveries/{id}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the webhook-out service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.webhookOut.deliveries.redeliver": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Accepted; the body is webhook-out's own redeliver response. console declares no response schema for this route, so no precise shape is asserted here — see the webhook-out service's own OpenAPI document for the authoritative body. */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.webhookOut.deliveries.cancel": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the webhook-out service's own `POST /v1/deliveries/{id}/cancel` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the webhook-out service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.webhookOut.events.list": {
        parameters: {
            query?: {
                q?: components["parameters"]["WebhookQ_q"];
                status?: components["parameters"]["WebhookQ_status"];
                event?: components["parameters"]["WebhookQ_event"];
                type?: components["parameters"]["WebhookQ_type"];
                subscription?: components["parameters"]["WebhookQ_subscription"];
                limit?: components["parameters"]["WebhookQ_limit"];
                cursor?: components["parameters"]["WebhookQ_cursor"];
                before?: components["parameters"]["WebhookQ_before"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the webhook-out service's own `GET /v1/events` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the webhook-out service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.webhookOut.events.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the webhook-out service's own `GET /v1/events/{id}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the webhook-out service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.search.stats.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the search service's own `GET /v1/stats` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the search service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.search.indexes.list": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the search service's own `GET /v1/indexes` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the search service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.search.indexes.create": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    name: string;
                    description?: string;
                    weights?: {
                        title?: number;
                        body?: number;
                        tags?: number;
                    };
                    facets?: string[];
                };
            };
        };
        responses: {
            /** @description Created; the body is search's own create response. console declares no response schema for this route, so no precise shape is asserted here — see the search service's own OpenAPI document for the authoritative body. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.search.indexes.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the search service's own `GET /v1/indexes/{name}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the search service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.search.indexes.delete": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. console discards search's response body. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.search.indexes.update": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    description?: string;
                    weights?: {
                        title?: number;
                        body?: number;
                        tags?: number;
                    };
                    facets?: string[];
                };
            };
        };
        responses: {
            /** @description Pass-through of the search service's own `PATCH /v1/indexes/{name}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the search service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.search.indexes.clear": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the search service's own `POST /v1/indexes/{name}/clear` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the search service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.search.indexes.search": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        /** @description Every field is optional; an absent body is forwarded as `{}`. */
        requestBody?: {
            content: {
                "application/json": {
                    q?: string;
                    filters?: {
                        [key: string]: string[];
                    };
                    facets?: string[];
                    limit?: number;
                    offset?: number;
                    highlight?: boolean;
                    /** @enum {string} */
                    sort?: "relevance" | "newest" | "oldest";
                };
            };
        };
        responses: {
            /** @description Pass-through of the search service's own `POST /v1/indexes/{name}/search` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the search service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.search.documents.browse": {
        parameters: {
            query?: {
                limit?: components["parameters"]["OffsetQ_limit"];
                offset?: components["parameters"]["OffsetQ_offset"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the search service's own `GET /v1/indexes/{name}/documents` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the search service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.search.documents.upsert": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        /** @description Batch of at most 500 documents. Only the `documents` array is forwarded. */
        requestBody: {
            content: {
                "application/json": {
                    documents: {
                        id: string;
                        title: string;
                        body?: string;
                        tags?: string[];
                        attrs?: {
                            [key: string]: unknown;
                        };
                        url?: string;
                    }[];
                };
            };
        };
        responses: {
            /** @description Pass-through of the search service's own `PUT /v1/indexes/{name}/documents` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the search service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.search.documents.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
                /** @description Second-level downstream resource id (session, environment, document, override, subject, place or variant), passed through URL-encoded. */
                sub: components["parameters"]["SubParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the search service's own `GET /v1/indexes/{name}/documents/{docId}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the search service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.search.documents.delete": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
                /** @description Second-level downstream resource id (session, environment, document, override, subject, place or variant), passed through URL-encoded. */
                sub: components["parameters"]["SubParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. console discards search's response body. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.ratelimit.stats.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the ratelimit service's own `GET /v1/stats` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the ratelimit service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.ratelimit.policies.list": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the ratelimit service's own `GET /v1/policies` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the ratelimit service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.ratelimit.policies.create": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    name: string;
                    description?: string;
                    limits: {
                        /** @description Window length in seconds. */
                        window: number;
                        /** @description Allowed cost per window; `0` blocks everything. */
                        limit: number;
                    }[];
                };
            };
        };
        responses: {
            /** @description Created; the body is ratelimit's own create response. console declares no response schema for this route, so no precise shape is asserted here — see the ratelimit service's own OpenAPI document for the authoritative body. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.ratelimit.policies.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the ratelimit service's own `GET /v1/policies/{name}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the ratelimit service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.ratelimit.policies.delete": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. console discards ratelimit's response body. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.ratelimit.policies.update": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    description?: string;
                    limits?: {
                        /** @description Window length in seconds. */
                        window: number;
                        /** @description Allowed cost per window; `0` blocks everything. */
                        limit: number;
                    }[];
                };
            };
        };
        responses: {
            /** @description Pass-through of the ratelimit service's own `PATCH /v1/policies/{name}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the ratelimit service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.ratelimit.policies.stats": {
        parameters: {
            query?: {
                hours?: components["parameters"]["RlStatsQ_hours"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the ratelimit service's own `GET /v1/policies/{name}/stats` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the ratelimit service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.ratelimit.policies.top": {
        parameters: {
            query?: {
                window?: components["parameters"]["RlTopQ_window"];
                limit?: components["parameters"]["RlTopQ_limit"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the ratelimit service's own `GET /v1/policies/{name}/top` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the ratelimit service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.ratelimit.overrides.list": {
        parameters: {
            query?: {
                limit?: components["parameters"]["OffsetQ_limit"];
                offset?: components["parameters"]["OffsetQ_offset"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the ratelimit service's own `GET /v1/policies/{name}/overrides` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the ratelimit service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.ratelimit.overrides.set": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
                /** @description Second-level downstream resource id (session, environment, document, override, subject, place or variant), passed through URL-encoded. */
                sub: components["parameters"]["SubParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    limits: {
                        /** @description Window length in seconds. */
                        window: number;
                        /** @description Allowed cost per window; `0` blocks everything. */
                        limit: number;
                    }[];
                    note?: string;
                    expiresAt?: string | null;
                };
            };
        };
        responses: {
            /** @description Pass-through of the ratelimit service's own `PUT /v1/policies/{name}/overrides/{subject}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the ratelimit service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.ratelimit.overrides.delete": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
                /** @description Second-level downstream resource id (session, environment, document, override, subject, place or variant), passed through URL-encoded. */
                sub: components["parameters"]["SubParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Removed. console discards ratelimit's response body. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.ratelimit.subjects.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
                /** @description Second-level downstream resource id (session, environment, document, override, subject, place or variant), passed through URL-encoded. */
                sub: components["parameters"]["SubParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the ratelimit service's own `GET /v1/policies/{name}/subjects/{subject}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the ratelimit service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.ratelimit.subjects.resetUsage": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
                /** @description Second-level downstream resource id (session, environment, document, override, subject, place or variant), passed through URL-encoded. */
                sub: components["parameters"]["SubParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the ratelimit service's own `DELETE /v1/policies/{name}/subjects/{subject}/usage` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the ratelimit service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.ratelimit.check.post": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    policy: string;
                    subject: string;
                    cost?: number;
                    /** @description When true the budget is only inspected, not consumed — and console then skips its own activity-log entry. */
                    peek?: boolean;
                };
            };
        };
        responses: {
            /** @description Pass-through of the ratelimit service's own `POST /v1/check` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the ratelimit service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.stats.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the geo service's own `GET /v1/stats` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.database.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the geo service's own `GET /v1/database` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.database.reload": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the geo service's own `POST /v1/database/reload` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.ip.lookup": {
        parameters: {
            query: {
                ip: components["parameters"]["GeoIpQ_ip"];
                /** @description BCP-47 language tag for localized names. */
                lang?: components["parameters"]["GeoIpQ_lang"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the geo service's own `GET /v1/ip` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.ip.batch": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    ips: string[];
                    lang?: string;
                };
            };
        };
        responses: {
            /** @description Pass-through of the geo service's own `POST /v1/ip/batch` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.countries.list": {
        parameters: {
            query?: {
                q?: components["parameters"]["GeoCountriesQ_q"];
                continent?: components["parameters"]["GeoCountriesQ_continent"];
                eu?: components["parameters"]["GeoCountriesQ_eu"];
                currency?: components["parameters"]["GeoCountriesQ_currency"];
                lang?: components["parameters"]["GeoCountriesQ_lang"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the geo service's own `GET /v1/countries` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.countries.get": {
        parameters: {
            query?: {
                country?: components["parameters"]["GeoTimezonesQ_country"];
                q?: components["parameters"]["GeoTimezonesQ_q"];
                lang?: components["parameters"]["GeoTimezonesQ_lang"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the geo service's own `GET /v1/countries/{code}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.currencies.list": {
        parameters: {
            query?: {
                country?: components["parameters"]["GeoTimezonesQ_country"];
                q?: components["parameters"]["GeoTimezonesQ_q"];
                lang?: components["parameters"]["GeoTimezonesQ_lang"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the geo service's own `GET /v1/currencies` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.timezones.list": {
        parameters: {
            query?: {
                country?: components["parameters"]["GeoTimezonesQ_country"];
                q?: components["parameters"]["GeoTimezonesQ_q"];
                lang?: components["parameters"]["GeoTimezonesQ_lang"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the geo service's own `GET /v1/timezones` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.phone.lookup": {
        parameters: {
            query: {
                number: components["parameters"]["GeoPhoneQ_number"];
                country?: components["parameters"]["GeoPhoneQ_country"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the geo service's own `GET /v1/phone` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.distance.get": {
        parameters: {
            query: {
                from: components["parameters"]["GeoDistanceQ_from"];
                to: components["parameters"]["GeoDistanceQ_to"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the geo service's own `GET /v1/distance` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.collections.list": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the geo service's own `GET /v1/collections` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.collections.create": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    name: string;
                    description?: string;
                };
            };
        };
        responses: {
            /** @description Created; the body is geo's own create response. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.collections.get": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the geo service's own `GET /v1/collections/{name}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.collections.delete": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. console discards geo's response body. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.collections.update": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    description?: string;
                };
            };
        };
        responses: {
            /** @description Pass-through of the geo service's own `PATCH /v1/collections/{name}` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.collections.clear": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the geo service's own `POST /v1/collections/{name}/clear` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.places.list": {
        parameters: {
            query?: {
                limit?: components["parameters"]["OffsetQ_limit"];
                offset?: components["parameters"]["OffsetQ_offset"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the geo service's own `GET /v1/collections/{name}/places` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.places.upsert": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        /** @description Batch of at most 5000 places. Only the `places` array is forwarded. */
        requestBody: {
            content: {
                "application/json": {
                    places: {
                        id: string;
                        name: string;
                        lat: number;
                        lng: number;
                        attrs?: {
                            [key: string]: unknown;
                        };
                    }[];
                };
            };
        };
        responses: {
            /** @description Pass-through of the geo service's own `PUT /v1/collections/{name}/places` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.places.delete": {
        parameters: {
            query?: never;
            header: {
                /** @description CSRF guard. `requireSession`/`requireAdmin` reject every non-GET/HEAD request whose value is not exactly `"1"` with `403 FORBIDDEN`. A browser cannot set a custom header cross-site without a CORS pre-flight the console never approves, which — together with the `SameSite=Strict` session cookie — is the whole CSRF defence. */
                "x-console-request": components["parameters"]["CsrfHeader"];
            };
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
                /** @description Second-level downstream resource id (session, environment, document, override, subject, place or variant), passed through URL-encoded. */
                sub: components["parameters"]["SubParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. console discards geo's response body. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
    "console.geo.collections.nearby": {
        parameters: {
            query: {
                lat: components["parameters"]["GeoNearbyQ_lat"];
                lng: components["parameters"]["GeoNearbyQ_lng"];
                radius?: components["parameters"]["GeoNearbyQ_radius"];
                limit?: components["parameters"]["GeoNearbyQ_limit"];
            };
            header?: never;
            path: {
                /** @description Service instance id, as configured in this deployment's `services.json`. Validated only by shape at the schema layer; whether the id exists — and whether the instance behind it is of the type this route needs — is checked at call time and both failures produce the same `404 UNKNOWN_SERVICE`. The set of valid values is deployment configuration, not part of this contract, and differs per environment. */
                sid: components["parameters"]["SidParam"];
                /** @description Downstream resource id, passed through URL-encoded. */
                id: components["parameters"]["IdParam"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Pass-through of the geo service's own `GET /v1/collections/{name}/nearby` response body, relayed unchanged. console declares no response schema for this route, so no precise shape is asserted here — see the geo service's own OpenAPI document for the authoritative body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PassThrough"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthenticated"];
            404: components["responses"]["ProxyNotFound"];
            502: components["responses"]["Unreachable"];
            default: components["responses"]["DownstreamDefault"];
        };
    };
}
